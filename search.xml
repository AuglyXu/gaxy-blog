<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>setState到底是同步还是异步的</title>
    <url>/2020/07/25/setState/</url>
    <content><![CDATA[<h1 id="setState"><a href="#setState" class="headerlink" title="setState"></a>setState</h1><h4 id="setState同步还是异步"><a href="#setState同步还是异步" class="headerlink" title="setState同步还是异步"></a>setState同步还是异步</h4><ul>
<li>setState有时表现出异步,有时表现出同步,在<strong>合成事件</strong>和<strong>钩子函数</strong>中表现异步</li>
<li>setState批量更新优化是<strong>建立在合成事件和钩子函数</strong>之上的,在原生事件和setTimeout中不会发生异步.(顺序为setState =&gt; 钩子函数 =&gt; 合并更新状态)</li>
</ul>
<a id="more"></a>
<hr>
<h6 id="合成事件"><a href="#合成事件" class="headerlink" title="合成事件"></a>合成事件</h6><ul>
<li>React通过事件冒泡,将事件冒泡到document上面,再统一分发给中间层SyntheticEvent去执行事件</li>
</ul>
<h6 id="钩子函数"><a href="#钩子函数" class="headerlink" title="钩子函数"></a>钩子函数</h6><ul>
<li>钩子函数指生命周期的钩子</li>
</ul>
<hr>
<p><a href="https://www.jianshu.com/p/8d8f9aa4b033" target="_blank" rel="noopener">合成事件和钩子函数参考文档</a></p>
<h4 id="setState之后到底发生了什么"><a href="#setState之后到底发生了什么" class="headerlink" title="setState之后到底发生了什么"></a>setState之后到底发生了什么</h4><p>setState有两个注意事项:<br>官网提供:</p>
<ol>
<li>出于性能考虑，React 可能会把多个 setState() 调用<strong>合并成一个</strong>调用</li>
<li>因为 this.props 和 this.state 可能会异步更新，所以你<strong>不要依赖他们的值</strong>来更新下一个状态<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs JavaScript">add() &#123;<br>    <span class="hljs-keyword">this</span>.setState(&#123;<span class="hljs-attr">count</span>:count + <span class="hljs-number">1</span>&#125;)<br>    <span class="hljs-keyword">this</span>.setState(&#123;<span class="hljs-attr">count</span>:count + <span class="hljs-number">1</span>&#125;)<br>&#125;<br>add()<br></code></pre></td></tr></table></figure></li>
</ol>
<ul>
<li>上述连续两次在点击事件调用setState<h4 id="图例"><a href="#图例" class="headerlink" title="图例"></a>图例</h4><img src="setState%E6%B5%81%E7%A8%8B.png" alt=""><h5 id="执行顺序"><a href="#执行顺序" class="headerlink" title="执行顺序"></a>执行顺序</h5><h6 id="第一步"><a href="#第一步" class="headerlink" title="第一步"></a>第一步</h6></li>
<li>首先调用实例的updater上的enqueueSetState</li>
<li>目的是让setState入列<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs JavaScript">ReactComponent.prototype.setState = <span class="hljs-function"><span class="hljs-keyword">function</span> (<span class="hljs-params">partialState, callback</span>) </span>&#123;<br>  <span class="hljs-comment">//  将setState事务放进队列中</span><br>  <span class="hljs-keyword">this</span>.updater.enqueueSetState(<span class="hljs-keyword">this</span>, partialState);<br>  <span class="hljs-keyword">if</span> (callback) &#123;<br>      <span class="hljs-keyword">this</span>.updater.enqueueCallback(<span class="hljs-keyword">this</span>, callback, <span class="hljs-string">'setState'</span>);<br>  &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>
<h6 id="第二步"><a href="#第二步" class="headerlink" title="第二步"></a>第二步</h6></li>
<li>将更新状态的组件也放入队列中</li>
<li>将新的state放进数组里 </li>
<li>用enqueueUpdate来处理将要更新的实例对象</li>
</ul>
<h6 id="目前达到的效果"><a href="#目前达到的效果" class="headerlink" title="目前达到的效果"></a>目前达到的效果</h6><ul>
<li>setState在队列中</li>
<li>在这个setState执行过程里 我们将state放入了一个队列 将要更新的组件也放入了一个队列<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs JavaScript"> enqueueSetState: <span class="hljs-function"><span class="hljs-keyword">function</span> (<span class="hljs-params">publicInstance, partialState</span>) </span>&#123;<br> <br> <span class="hljs-comment">// 获取当前组件的instance</span><br><span class="hljs-keyword">var</span> internalInstance = getInternalInstanceReadyForUpdate(publicInstance, <span class="hljs-string">'setState'</span>);<br><br> <span class="hljs-comment">// 将要更新的state放入一个数组里</span><br> <span class="hljs-keyword">var</span> queue = internalInstance._pendingStateQueue || (internalInstance._pendingStateQueue = []);<br>queue.push(partialState);<br><br><span class="hljs-comment">//  将要更新的component instance也放在一个队列里</span><br>enqueueUpdate(internalInstance);<br>  &#125;<br></code></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="第三步"><a href="#第三步" class="headerlink" title="第三步"></a>第三步</h5><ul>
<li>调用enqueueUpdate执行更新操作,如果处在更新,那么只是将组件放入脏组件队列中<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs JavaScript"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">enqueueUpdate</span>(<span class="hljs-params">component</span>) </span>&#123;<br>  <span class="hljs-comment">// 如果没有处于批量创建/更新组件的阶段，则处理update state事务</span><br>  <span class="hljs-keyword">if</span> (!batchingStrategy.isBatchingUpdates) &#123;<br>    batchingStrategy.batchedUpdates(enqueueUpdate, component);<br>    <span class="hljs-keyword">return</span>;<br>  &#125;<br>  <span class="hljs-comment">// 如果正处于批量创建/更新组件的过程，将当前的组件放在dirtyComponents数组中</span><br>  dirtyComponents.push(component);<br>&#125;<br></code></pre></td></tr></table></figure></li>
<li>这个更新函数,接收一个组件的实例 首先判断是否处于更新阶段 如果不是 那么执行更新 如果处在更新阶段 那么我将组件的实例push到队列中 等待更新<h6 id="这个函数也说明了setState是一个异步的过程，它会集齐一批需要更新的组件然后一起更新"><a href="#这个函数也说明了setState是一个异步的过程，它会集齐一批需要更新的组件然后一起更新" class="headerlink" title="这个函数也说明了setState是一个异步的过程，它会集齐一批需要更新的组件然后一起更新"></a>这个函数也说明了setState是一个异步的过程，它会集齐一批需要更新的组件然后一起更新</h6></li>
</ul>
<h5 id="第三步拓展——批量更新策略-batchingStrategy"><a href="#第三步拓展——批量更新策略-batchingStrategy" class="headerlink" title="第三步拓展——批量更新策略(batchingStrategy)"></a>第三步拓展——批量更新策略(batchingStrategy)</h5><ul>
<li>第三步中,如果不处在更新状态,就执行<code>batchingStrategy.batchedUpdates(enqueueUpdate,component)</code></li>
</ul>
<h6 id="批量更新策略所在的对象"><a href="#批量更新策略所在的对象" class="headerlink" title="批量更新策略所在的对象"></a>批量更新策略所在的对象</h6><ul>
<li>batchedUpdates做了个判断,如果我处在更新状态,那么组件入列, 否则开启更新事务<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs JavaScript"><span class="hljs-keyword">var</span> ReactDefaultBatchingStrategy = &#123;<br>  <span class="hljs-comment">// 用于标记当前是否出于批量更新</span><br>  isBatchingUpdates: <span class="hljs-literal">false</span>,<br>  <span class="hljs-comment">// 当调用这个方法时，正式开始批量更新</span><br>  batchedUpdates: <span class="hljs-function"><span class="hljs-keyword">function</span> (<span class="hljs-params">callback, a, b, c, d, e</span>) </span>&#123;<br>    <span class="hljs-keyword">var</span> alreadyBatchingUpdates = ReactDefaultBatchingStrategy.isBatchingUpdates;<br><br>    ReactDefaultBatchingStrategy.isBatchingUpdates = <span class="hljs-literal">true</span>;<br><br><span class="hljs-comment">// 如果当前事务正在更新过程中，则调用enqueueUpdate</span><br>    <span class="hljs-keyword">if</span> (alreadyBatchingUpdates) &#123;<br>      <span class="hljs-keyword">return</span> callback(a, b, c, d, e);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>    <span class="hljs-comment">// 否则执行更新事务</span><br>      <span class="hljs-keyword">return</span> transaction.perform(callback, <span class="hljs-literal">null</span>, a, b, c, d, e);<br>    &#125;<br>  &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="第三步拓展——事务"><a href="#第三步拓展——事务" class="headerlink" title="第三步拓展——事务"></a>第三步拓展——事务</h5><ul>
<li><p>所谓事务,就是把函数做一层包装,开始是做一些操作,结束时做一些操作</p>
</li>
<li><p>下面是一个简单的事务例子</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs JavaScript"><span class="hljs-keyword">var</span> Transaction = <span class="hljs-built_in">require</span>(<span class="hljs-string">'./Transaction'</span>);<br><br><span class="hljs-comment">// 我们自己定义的 Transaction</span><br><span class="hljs-keyword">var</span> MyTransaction = <span class="hljs-function"><span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-comment">// do sth.</span><br>&#125;;<br><br><span class="hljs-built_in">Object</span>.assign(MyTransaction.prototype, Transaction.Mixin, &#123;<br>  getTransactionWrappers: <span class="hljs-function"><span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) </span>&#123;<br>    <span class="hljs-keyword">return</span> [&#123;<br>      initialize: <span class="hljs-function"><span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) </span>&#123;<br>        <span class="hljs-built_in">console</span>.log(<span class="hljs-string">'before method perform'</span>);<br>      &#125;,<br>      close: <span class="hljs-function"><span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) </span>&#123;<br>        <span class="hljs-built_in">console</span>.log(<span class="hljs-string">'after method perform'</span>);<br>      &#125;<br>    &#125;];<br>  &#125;;<br>&#125;);<br><br><span class="hljs-keyword">var</span> transaction = <span class="hljs-keyword">new</span> MyTransaction();<br><span class="hljs-keyword">var</span> testMethod = <span class="hljs-function"><span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-built_in">console</span>.log(<span class="hljs-string">'test'</span>);<br>&#125;<br>transaction.perform(testMethod);<br><br><span class="hljs-comment">// before method perform</span><br><span class="hljs-comment">// test</span><br><span class="hljs-comment">// after method perform</span><br></code></pre></td></tr></table></figure>
<p><img src="setState%E4%BA%8B%E5%8A%A1.png" alt=""></p>
</li>
<li><p>React源码的事务中有两个事务RESET_BATCHED_UPDATES(用于重置更新状态) FLUSH_BATCHED_UPDATES(再发起一个dom的批量更新,包括渲染和虚拟dom比对等等)</p>
</li>
<li><p>FLUSH_BATCHED_UPDATES这个事务的目的在于循环所有更新组件,执行update,调用组件更新的生命周期等等</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs JavaScript"><span class="hljs-keyword">var</span> RESET_BATCHED_UPDATES = &#123;<br>  initialize: emptyFunction,<br>  close: <span class="hljs-function"><span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) </span>&#123;<br>    ReactDefaultBatchingStrategy.isBatchingUpdates = <span class="hljs-literal">false</span>;<br>  &#125;<br>&#125;;<br><br><span class="hljs-keyword">var</span> FLUSH_BATCHED_UPDATES = &#123;<br>  initialize: emptyFunction,<br>  close: ReactUpdates.flushBatchedUpdates.bind(ReactUpdates)<br>&#125;;<br></code></pre></td></tr></table></figure>


</li>
</ul>
<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><ul>
<li>setState第一步:<strong>将state塞入数组,将组件交给更新队列</strong></li>
<li>setState第二步:更新队列进行更新,如果更新队列<strong>正在更新</strong>,将组件<strong>塞入脏组件队列</strong>.否则<strong>执行批量更新策略</strong></li>
<li>setState第三步:批量更新策略<strong>判断是否处在批量更新状态</strong>,如果<strong>正在更新</strong>,那么将<strong>更新组件塞入脏组件队列</strong>,否则把脏组件所有组件拿出来<strong>开启事务更新</strong></li>
<li>setState第四步,批量更新结束事务,在事务结束前,会调用FLUSH_BATCHED_UPDATES<strong>计算最新的<code>state</code>和<code>props</code></strong>.然后关闭事务</li>
<li>然后React可以拿到最新的state和props进行虚拟dom比对,虚拟dom比对完之后,会去渲染组件,此时如果有shouldUpdate,会进行一个是否渲染组件的判断</li>
</ul>
<h4 id="为什么setTimeout下setState是同步的"><a href="#为什么setTimeout下setState是同步的" class="headerlink" title="为什么setTimeout下setState是同步的"></a>为什么setTimeout下setState是同步的</h4><ul>
<li>关键在于批量更新策略什么时候开启的</li>
<li>React开启批量更新策略有两个位置,一个是钩子函数,一个是合成事件,可以将钩子函数和合成事件理解为一个大的事务,当触发时,React会开启批量更新策略.而当执行setTimeout的时候,大事务已经关闭,批量更新策略已经重置为false</li>
<li>实际上,调用setState触发了两个批量更新事务,相当于同步更新的过程了.</li>
</ul>
<h4 id="面试问答"><a href="#面试问答" class="headerlink" title="面试问答"></a>面试问答</h4><ul>
<li>问: setState过后到底发生了什么?</li>
<li>答: 将组件交给更新队列,将state塞入数组,然后执行队列更新方法,如果队列正在更新,组件就塞入脏组件,否则执行批量更新策略, 批量更新策略会去判断是否正在批量更新,如果正在更新,会把队列放入脏组件,否则循环脏组件队列开启事务执行批量更新,然后在事务结束前计算最新state和props,React拿到他们去进行虚拟dom比对,组件渲染,判断shouldUpdate,进行更新.更新完执行componentDidUpdate…</li>
</ul>
<p><a href="https://juejin.im/post/5aa25967518825558251f61f" target="_blank" rel="noopener">React - setState源码分析（小白可读）</a><br><a href="https://imweb.io/topic/5b189d04d4c96b9b1b4c4ed6" target="_blank" rel="noopener">揭密React setState</a><br><a href="https://juejin.im/post/5d7f219a51882501734c2921" target="_blank" rel="noopener">react源码分析之-setState是异步还是同步？</a></p>
]]></content>
      <categories>
        <category>React</category>
      </categories>
      <tags>
        <tag>React</tag>
        <tag>前端</tag>
      </tags>
  </entry>
  <entry>
    <title>重学Hooks——useEffect</title>
    <url>/2020/07/26/useEffect/</url>
    <content><![CDATA[<h1 id="useEffect"><a href="#useEffect" class="headerlink" title="useEffect"></a>useEffect</h1><blockquote>
<p>Dan在OverReact上发表的文章深入浅出，本文只针对个人之前不理解的点进行思考，采用了他的案例，参考了他的文章——<a href="https://overreacted.io/zh-hans/a-complete-guide-to-useeffect/" target="_blank" rel="noopener">useEffect的完整指南</a>.</p>
</blockquote>
<p>在我看来，Effect hook是React Hooks中最强大最核心的一个hook，是驱动整个程序的纽带我也将采用Dan的案例进行思考</p>
<hr>
<ul>
<li>根据文章问题，进行思考后，抛出了以下几个问题，将在下文对这几个问题做详细的思考<ol>
<li>Effect是如何进行渲染的?</li>
<li>如何用Effect模拟React的生命周期？<code>useEffect(fn,[])</code>和<code>componentDidMount</code>一样吗？</li>
<li>如何正确的使用Effect请求数据？</li>
<li>Effect的依赖到底用什么，可以用函数嘛，什么时候用函数作为依赖？</li>
<li>Effect Hook怎么会导致死循环？</li>
<li>Effect Hook怎么会拿到旧state和props，如果我真的想用旧的state和props，我应该怎么去获取？</li>
</ol>
</li>
</ul>
<a id="more"></a>

<h2 id="Effect到底是如何渲染的"><a href="#Effect到底是如何渲染的" class="headerlink" title="Effect到底是如何渲染的?"></a>Effect到底是如何渲染的?</h2><h3 id="渲染中state的渲染"><a href="#渲染中state的渲染" class="headerlink" title="渲染中state的渲染"></a>渲染中state的渲染</h3><ul>
<li>以下是最简单的点击次数加一的事件， 分析一下点击后数字的改变</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Counter</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><br>  <span class="hljs-keyword">return</span> (<br>    &lt;div&gt;<br>      &lt;p&gt;You clicked &#123;count&#125; times&lt;<span class="hljs-regexp">/p&gt;</span><br><span class="hljs-regexp">      &lt;button onClick=&#123;() =&gt; setCount(count + 1)&#125;&gt;</span><br><span class="hljs-regexp">        Click me</span><br><span class="hljs-regexp">      &lt;/</span>button&gt;<br>    &lt;<span class="hljs-regexp">/div&gt;</span><br><span class="hljs-regexp">  );</span><br><span class="hljs-regexp">&#125;</span><br></code></pre></td></tr></table></figure>
<p>以下展示了count改变的来源，并非是通过事件监听或是事件绑定或是代理等对count本身做出的改变，而是<strong>重新创建了一个count</strong>， 新创建的count值是最后一次改变的state中的count.</p>
<table>
<thead>
<tr>
<th>点击次数</th>
<th>值</th>
<th>count来源</th>
</tr>
</thead>
<tbody><tr>
<td>0</td>
<td>0</td>
<td>useState默认值</td>
</tr>
<tr>
<td>1</td>
<td>1</td>
<td>上一个useState的返回值</td>
</tr>
<tr>
<td>2</td>
<td>2</td>
<td>上一个useState的返回值</td>
</tr>
</tbody></table>
<p>代码体现则是如下</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// During first render</span><br><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Counter</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> count = <span class="hljs-number">0</span>; <span class="hljs-comment">// Returned by useState()</span><br>  <span class="hljs-comment">// ...</span><br>  &lt;p&gt;You clicked &#123;count&#125; times&lt;<span class="hljs-regexp">/p&gt;</span><br><span class="hljs-regexp">  /</span><span class="hljs-regexp">/ ...</span><br><span class="hljs-regexp">&#125;</span><br><span class="hljs-regexp"></span><br><span class="hljs-regexp">/</span><span class="hljs-regexp">/ After a click, our function is called again</span><br><span class="hljs-regexp">function Counter() &#123;</span><br><span class="hljs-regexp">  const count = 1; /</span><span class="hljs-regexp">/ Returned by useState()</span><br><span class="hljs-regexp">  /</span><span class="hljs-regexp">/ ...</span><br><span class="hljs-regexp">  &lt;p&gt;You clicked &#123;count&#125; times&lt;/</span>p&gt;<br>  <span class="hljs-comment">// ...</span><br>&#125;<br><br><span class="hljs-comment">// After another click, our function is called again</span><br><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Counter</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> count = <span class="hljs-number">2</span>; <span class="hljs-comment">// Returned by useState()</span><br>  <span class="hljs-comment">// ...</span><br>  &lt;p&gt;You clicked &#123;count&#125; times&lt;<span class="hljs-regexp">/p&gt;</span><br><span class="hljs-regexp">  /</span><span class="hljs-regexp">/ ...</span><br><span class="hljs-regexp">&#125;</span><br></code></pre></td></tr></table></figure>
<ul>
<li>因此发现，其实count只是一个常量，React在使用<code>setCount</code>后，带着一个新count再次调用组件!<blockquote>
<p>至于更深入的研究，还未研究过，准备参考Dan的另一篇文章<a href="https://overreacted.io/zh-hans/react-as-a-ui-runtime/" target="_blank" rel="noopener">将 React 作为 UI 运行时</a></p>
</blockquote>
</li>
</ul>
<h3 id="渲染中事件处理函数的渲染"><a href="#渲染中事件处理函数的渲染" class="headerlink" title="渲染中事件处理函数的渲染"></a>渲染中事件处理函数的渲染</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Counter</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><br>  <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">handleAlertClick</span>(<span class="hljs-params"></span>) </span>&#123;<br>    setTimeout(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>      alert(<span class="hljs-string">'You clicked on: '</span> + count);<br>    &#125;, <span class="hljs-number">3000</span>);<br>  &#125;<br><br>  <span class="hljs-keyword">return</span> (<br>    &lt;div&gt;<br>      &lt;p&gt;You clicked &#123;count&#125; times&lt;<span class="hljs-regexp">/p&gt;</span><br><span class="hljs-regexp">      &lt;button onClick=&#123;() =&gt; setCount(count + 1)&#125;&gt;</span><br><span class="hljs-regexp">        Click me</span><br><span class="hljs-regexp">      &lt;/</span>button&gt;<br>      &lt;button onClick=&#123;handleAlertClick&#125;&gt;<br>        Show alert<br>      &lt;<span class="hljs-regexp">/button&gt;</span><br><span class="hljs-regexp">    &lt;/</span>div&gt;<br>  );<br>&#125;<br></code></pre></td></tr></table></figure>
<ul>
<li>我们按照以下步骤做<ol>
<li>我先点击到按钮，使count到达1</li>
<li>点击show alert，再3秒内迅速点击按钮使count到3</li>
<li>观察alert的值， 值为1 or 3 ?? </li>
</ol>
</li>
</ul>
<p><a href="https://codesandbox.io/s/w2wxl3yo0l" target="_blank" rel="noopener">点我操作，看看到底是个啥</a></p>
<ul>
<li>根据上文，以下展示了这个调用的情况</li>
</ul>
<table>
<thead>
<tr>
<th>是否点击alert</th>
<th>此时点击次数</th>
<th>值</th>
<th>count来源</th>
<th>handleAlertClick</th>
</tr>
</thead>
<tbody><tr>
<td>否</td>
<td>0</td>
<td>0</td>
<td>useState默认值</td>
<td>handleAlertClick中的count取0</td>
</tr>
<tr>
<td>否</td>
<td>1</td>
<td>1</td>
<td>上一个useState的返回值</td>
<td>handleAlertClick中的count取1</td>
</tr>
<tr>
<td>是</td>
<td>1</td>
<td>1</td>
<td>上一个useState的返回值</td>
<td>handleAlertClick中的count取1</td>
</tr>
<tr>
<td>否</td>
<td>2</td>
<td>2</td>
<td>上一个useState的返回值</td>
<td>handleAlertClick中的count取2</td>
</tr>
<tr>
<td>否</td>
<td>3</td>
<td>3</td>
<td>上一个useState的返回值</td>
<td>handleAlertClick中的count取3</td>
</tr>
<tr>
<td>alert弹出</td>
<td>…</td>
<td>…</td>
<td>…</td>
<td>弹出表格对应的第三行的handleAlertClick</td>
</tr>
</tbody></table>
<ul>
<li>我们发现，每次调用的count和handleAlertClick，都是<strong>重新创建的<code>count</code>和<code>handleAlertClick</code></strong>，每次重新渲染组件，上一次的栈内存都将被释放。由于闭包，第一次的count并未被释放，而handleAlertClick被存放在了任务队列，记录的是没有被释放的count，哪怕点到了3，与之前的也没有任何关系，每次渲染都是<strong>独立</strong>的，因此值是1。</li>
</ul>
<p><em>每次渲染的state和props在渲染中是不会被改变的，因此每次渲染都是独立的，每次渲染的state和props都是不同的state和props。这种独立关系，再修改引用类型时，希望我们<code>setObject(newObject)</code>，这样可以保证上一个state不被污染</em></p>
<h4 id="问题1：Effect清理与浏览器渲染屏幕的执行顺序是什么样的呢？"><a href="#问题1：Effect清理与浏览器渲染屏幕的执行顺序是什么样的呢？" class="headerlink" title="问题1：Effect清理与浏览器渲染屏幕的执行顺序是什么样的呢？"></a>问题1：Effect清理与浏览器渲染屏幕的执行顺序是什么样的呢？</h4><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// First render, props are &#123;id: 10&#125;</span><br><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Example</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-comment">// ...</span><br>  useEffect(<br>    <span class="hljs-comment">// Effect from first render</span><br>    () =&gt; &#123;<br>      ChatAPI.subscribeToFriendStatus(<span class="hljs-number">10</span>, handleStatusChange);<br>      <span class="hljs-comment">// Cleanup for effect from first render</span><br>      <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>        ChatAPI.unsubscribeFromFriendStatus(<span class="hljs-number">10</span>, handleStatusChange);<br>      &#125;;<br>    &#125;<br>  );<br>  <span class="hljs-comment">// ...</span><br>&#125;<br><br><span class="hljs-comment">// Next render, props are &#123;id: 20&#125;</span><br><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Example</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-comment">// ...</span><br>  useEffect(<br>    <span class="hljs-comment">// Effect from second render</span><br>    () =&gt; &#123;<br>      ChatAPI.subscribeToFriendStatus(<span class="hljs-number">20</span>, handleStatusChange);<br>      <span class="hljs-comment">// Cleanup for effect from second render</span><br>      <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>        ChatAPI.unsubscribeFromFriendStatus(<span class="hljs-number">20</span>, handleStatusChange);<br>      &#125;;<br>    &#125;<br>  );<br>  <span class="hljs-comment">// ...</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>按照常理的逻辑，是这个顺序吗？？？？？？？</p>
<table>
<thead>
<tr>
<th>次数</th>
<th>操作</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>渲染props.id为10的UI</td>
</tr>
<tr>
<td>2</td>
<td>执行Effect，订阅数据</td>
</tr>
<tr>
<td>3</td>
<td>清除id为10的Effect</td>
</tr>
<tr>
<td>4</td>
<td>渲染props.id为20的UI</td>
</tr>
<tr>
<td>5</td>
<td>执行Effect，订阅数据</td>
</tr>
<tr>
<td>yes？？？？</td>
<td></td>
</tr>
<tr>
<td><strong>No</strong></td>
<td></td>
</tr>
</tbody></table>
<p>结果应该是如下的</p>
<table>
<thead>
<tr>
<th>次数</th>
<th>操作</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>渲染props.id为10的UI</td>
</tr>
<tr>
<td>2</td>
<td>执行Effect，订阅数据</td>
</tr>
<tr>
<td>3</td>
<td>渲染props.id为20的UI</td>
</tr>
<tr>
<td>4</td>
<td>清除id为10的Effect</td>
</tr>
<tr>
<td>5</td>
<td>执行Effect，订阅数据</td>
</tr>
</tbody></table>
<p>因为Effect的执行一定是放在<strong>浏览器渲染屏幕</strong>之后的！因为每次渲染都是独立的，上一个Effect只能记住id为10的状态，因此，effect的清除并不会读取<strong>最新</strong>的<code>props</code>。它只能读取到定义它的那次渲染中的<code>props</code>值。</p>
<h4 id="问题2：每次渲染的Effect都是不同的Effect嘛？那么Effect中的state和外部state是什么关系？"><a href="#问题2：每次渲染的Effect都是不同的Effect嘛？那么Effect中的state和外部state是什么关系？" class="headerlink" title="问题2：每次渲染的Effect都是不同的Effect嘛？那么Effect中的state和外部state是什么关系？"></a>问题2：每次渲染的Effect都是不同的Effect嘛？那么Effect中的state和外部state是什么关系？</h4><p><strong>每次渲染的Effect都是不同的Effect</strong><br><strong>Effect中的state和props，都是特定的那次渲染的state和props</strong></p>
<h5 id="React执行Effect的时机是什么？"><a href="#React执行Effect的时机是什么？" class="headerlink" title="React执行Effect的时机是什么？"></a>React执行Effect的时机是什么？</h5><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Counter</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br>    <span class="hljs-comment">// console.log('effect外部被执行了')</span><br>    useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>        <span class="hljs-comment">// console.log('effect内部被执行了')</span><br>        <span class="hljs-built_in">document</span>.title = count<br>    &#125;)<br>  <span class="hljs-keyword">return</span> (<br>    &lt;div&gt;<br>      &lt;p&gt;You clicked &#123;count&#125; times&lt;<span class="hljs-regexp">/p&gt;</span><br><span class="hljs-regexp">      &lt;button onClick=&#123;() =&gt; setCount(count + 1)&#125;&gt;</span><br><span class="hljs-regexp">        Click me</span><br><span class="hljs-regexp">      &lt;/</span>button&gt;<br>    &lt;<span class="hljs-regexp">/div&gt;</span><br><span class="hljs-regexp">  );</span><br><span class="hljs-regexp">&#125;</span><br></code></pre></td></tr></table></figure>
<ul>
<li>执行顺序如下<ol>
<li>执行<code>const [count, setCount] = useState(0);</code></li>
<li>React记住Effect</li>
<li><strong>渲染dom</strong></li>
<li>调用<code>document.title = count</code></li>
</ol>
</li>
<li>因此我们要记住，<strong>Effect是在每次更改作用于DOM并让<em>浏览器绘制屏幕后</em>去调用它</strong></li>
</ul>
<h3 id="Effect中的异步"><a href="#Effect中的异步" class="headerlink" title="Effect中的异步"></a>Effect中的异步</h3><p>针对同步的情况，已经了解的差不多了，那么如果Effect中有延迟呢？</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Counter</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><br>  useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    setTimeout(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>      <span class="hljs-built_in">console</span>.log(<span class="hljs-string">`You clicked <span class="hljs-subst">$&#123;count&#125;</span> times`</span>);<br>    &#125;, <span class="hljs-number">3000</span>);<br>  &#125;);<br><br>  <span class="hljs-keyword">return</span> (<br>    &lt;div&gt;<br>      &lt;p&gt;You clicked &#123;count&#125; times&lt;<span class="hljs-regexp">/p&gt;</span><br><span class="hljs-regexp">      &lt;button onClick=&#123;() =&gt; setCount(count + 1)&#125;&gt;</span><br><span class="hljs-regexp">        Click me</span><br><span class="hljs-regexp">      &lt;/</span>button&gt;<br>    &lt;<span class="hljs-regexp">/div&gt;</span><br><span class="hljs-regexp">  );</span><br><span class="hljs-regexp">&#125;</span><br></code></pre></td></tr></table></figure>
<p>假如我点击三次，将会打印什么，结论如下，原因很简单，因为每一个Effect中保存的是当前的count</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs plain">You clicked 0 times<br>You clicked 1 times<br>You clicked 2 times<br>You clicked 3 times<br></code></pre></td></tr></table></figure>

<p>但是！上述工作机制与类并不相同，hooks写法中，每一个count是独立的，类写法中，将会输出一次<code>you clicked 0 times</code>和3次<code>you clicked 3 times</code>，原因是因为类写法中的count是同一个count</p>
<p><em>这原来就是困扰我好久好久的Effect中的闭包啊</em></p>
<h4 id="问题来了，如果我就想打印3次you-clicked-3-times怎么办"><a href="#问题来了，如果我就想打印3次you-clicked-3-times怎么办" class="headerlink" title="问题来了，如果我就想打印3次you clicked 3 times怎么办"></a>问题来了，如果我就想打印3次<code>you clicked 3 times</code>怎么办</h4><h5 id="ref登场"><a href="#ref登场" class="headerlink" title="ref登场"></a>ref登场</h5><ul>
<li>不同于class中的ref，hooks中的ref不仅可以保存dom元素，他可以作为<strong>任何值</strong>的容器<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Example</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br>  <span class="hljs-keyword">const</span> latestCount = useRef(count);<br><br>  useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    <span class="hljs-comment">// Set the mutable latest value</span><br>    latestCount.current = count;<br>    setTimeout(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>      <span class="hljs-comment">// Read the mutable latest value</span><br>      <span class="hljs-built_in">console</span>.log(<span class="hljs-string">`You clicked <span class="hljs-subst">$&#123;latestCount.current&#125;</span> times`</span>);<br>    &#125;, <span class="hljs-number">3000</span>);<br>  &#125;);<br></code></pre></td></tr></table></figure></li>
<li>ok，这样就搞定啦，官网说了， <code>useRef</code>返回的对象，在<strong>整个生命周期内</strong>保持不变，因此不用担心每次创建的都是新的ref，这样我Effect函数中改变的ref内的容器值，都是同一个。</li>
<li>文章上半部分已经可以解决我们开篇提到的1、2、5、6三个问题<h2 id="Effect到底是怎么更新的？"><a href="#Effect到底是怎么更新的？" class="headerlink" title="Effect到底是怎么更新的？"></a>Effect到底是怎么更新的？</h2></li>
</ul>
<hr>
<h3 id="告诉React你要做什么样的比对"><a href="#告诉React你要做什么样的比对" class="headerlink" title="告诉React你要做什么样的比对"></a>告诉React你要做什么样的比对</h3><ul>
<li>我想关于这个依赖项，React文档做了更详尽的阐述。这里只拎几个点出来</li>
</ul>
<h4 id="类似于setState的函数式更新"><a href="#类似于setState的函数式更新" class="headerlink" title="类似于setState的函数式更新"></a>类似于setState的函数式更新</h4><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Counter</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><br>  useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    <span class="hljs-keyword">const</span> id = setInterval(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>      setCount(count + <span class="hljs-number">1</span>);<br>    &#125;, <span class="hljs-number">1000</span>);<br>    <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> clearInterval(id);<br>  &#125;, []);<br><br>  <span class="hljs-keyword">return</span> <span class="xml"><span class="hljs-tag">&lt;<span class="hljs-name">h1</span>&gt;</span>&#123;count&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">h1</span>&gt;</span></span>;<br>&#125;<br></code></pre></td></tr></table></figure>
<ul>
<li>由于每次渲染都是独立的，我们知道，这里count永远都是1</li>
<li>解决方案如下：<ul>
<li>1、设置count作为依赖项<ul>
<li>虽然解决了问题，但是非常不好，代码如下，原因是因为每次修改count，都将重新生成一个定时器，useEffect都会被重新执行，这显然不是我们想要的结果</li>
</ul>
</li>
<li>2、函数式更新<ul>
<li>比较理想的解决方案</li>
</ul>
</li>
</ul>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// 设置count为依赖项</span><br><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Counter</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><br>  useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    <span class="hljs-keyword">const</span> id = setInterval(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>      setCount(count + <span class="hljs-number">1</span>);<br>    &#125;, <span class="hljs-number">1000</span>);<br>    <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> clearInterval(id);<br>  &#125;, [count]);<br><br>  <span class="hljs-keyword">return</span> <span class="xml"><span class="hljs-tag">&lt;<span class="hljs-name">h1</span>&gt;</span>&#123;count&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">h1</span>&gt;</span></span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// 函数式更新</span><br><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Counter</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br><br>  useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    <span class="hljs-keyword">const</span> id = setInterval(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>      setCount(<span class="hljs-function"><span class="hljs-params">c</span> =&gt;</span> c + <span class="hljs-number">1</span>);<br>    &#125;, <span class="hljs-number">1000</span>);<br>    <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> clearInterval(id);<br>  &#125;, []);<br><br>  <span class="hljs-keyword">return</span> <span class="xml"><span class="hljs-tag">&lt;<span class="hljs-name">h1</span>&gt;</span>&#123;count&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">h1</span>&gt;</span></span>;<br>&#125;<br></code></pre></td></tr></table></figure>
<ul>
<li>这种函数式更新，无需知道count的值，React已经知道，并将最新的count传递进去。成功将依赖项count移除</li>
</ul>
<p><strong>但是</strong></p>
<p>我不仅想要知道最新的count，我还想要知道最新的<code>props</code>或是其他的<code>state</code>。。（函数式更新凉凉）</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Counter</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [count, setCount] = useState(<span class="hljs-number">0</span>);<br>  <span class="hljs-keyword">const</span> [step, setStep] = useState(<span class="hljs-number">1</span>);<br><br>  useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    <span class="hljs-keyword">const</span> id = setInterval(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>      setCount(<span class="hljs-function"><span class="hljs-params">c</span> =&gt;</span> c + step);<br>    &#125;, <span class="hljs-number">1000</span>);<br>    <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> clearInterval(id);<br>  &#125;, [step]);<br><br>  <span class="hljs-keyword">return</span> (<br>    &lt;&gt;<br>      &lt;h1&gt;&#123;count&#125;&lt;<span class="hljs-regexp">/h1&gt;</span><br><span class="hljs-regexp">      &lt;input value=&#123;step&#125; onChange=&#123;e =&gt; setStep(Number(e.target.value))&#125; /</span>&gt;<br>    &lt;<span class="hljs-regexp">/&gt;</span><br><span class="hljs-regexp">  );</span><br><span class="hljs-regexp">&#125;</span><br></code></pre></td></tr></table></figure>
<p>炸了，不是我们想要的</p>
<h3 id="useReducer"><a href="#useReducer" class="headerlink" title="useReducer"></a>useReducer</h3><ul>
<li>首先我们要知道，什么时候用这个useReducer？这里引用了React文档的内容。<blockquote>
<p>在某些场景下，<code>useReducer</code> 会比 <code>useState</code> 更适用，例如 <code>state</code> <strong>逻辑较复杂</strong>且包含多个子值，或者<strong>下一个 <code>state</code> 依赖于之前的 <code>state</code></strong> 等。并且，使用 useReducer 还能给那些会触发深更新的组件做性能优化，因为你可以向子组件传递 dispatch 而不是回调函数。</p>
</blockquote>
</li>
</ul>
<p>这里默认已经会Redux了。</p>
<ul>
<li>关键点：<ul>
<li><ol>
<li>逻辑复杂</li>
</ol>
</li>
<li><ol start="2">
<li><strong>状态依赖</strong></li>
</ol>
</li>
<li><ol start="3">
<li>嵌套深的组件性能优化<br>其中第二点就是我们说的那一点，因此上面的案例可以改写为:</li>
</ol>
</li>
</ul>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Counter</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [state, dispatch] = useReducer(reducer, initialState);<br>  <span class="hljs-keyword">const</span> &#123; count, step &#125; = state;<br><br>  useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    <span class="hljs-keyword">const</span> id = setInterval(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>      dispatch(&#123; <span class="hljs-attr">type</span>: <span class="hljs-string">'tick'</span> &#125;);<br>    &#125;, <span class="hljs-number">1000</span>);<br>    <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> clearInterval(id);<br>  &#125;, [dispatch]);<br><br>  <span class="hljs-keyword">return</span> (<br>    &lt;&gt;<br>      &lt;h1&gt;&#123;count&#125;&lt;<span class="hljs-regexp">/h1&gt;</span><br><span class="hljs-regexp">      &lt;input value=&#123;step&#125; onChange=&#123;e =&gt; &#123;</span><br><span class="hljs-regexp">        dispatch(&#123;</span><br><span class="hljs-regexp">          type: 'step',</span><br><span class="hljs-regexp">          step: Number(e.target.value)</span><br><span class="hljs-regexp">        &#125;);</span><br><span class="hljs-regexp">      &#125;&#125; /</span>&gt;<br>    &lt;<span class="hljs-regexp">/&gt;</span><br><span class="hljs-regexp">  );</span><br><span class="hljs-regexp">&#125;</span><br><span class="hljs-regexp"></span><br><span class="hljs-regexp">const initialState = &#123;</span><br><span class="hljs-regexp">  count: 0,</span><br><span class="hljs-regexp">  step: 1,</span><br><span class="hljs-regexp">&#125;;</span><br><span class="hljs-regexp"></span><br><span class="hljs-regexp">function reducer(state, action) &#123;</span><br><span class="hljs-regexp">  const &#123; count, step &#125; = state;</span><br><span class="hljs-regexp">  if (action.type === 'tick') &#123;</span><br><span class="hljs-regexp">    return &#123; count: count + step, step &#125;;</span><br><span class="hljs-regexp">  &#125; else if (action.type === 'step') &#123;</span><br><span class="hljs-regexp">    return &#123; count, step: action.step &#125;;</span><br><span class="hljs-regexp">  &#125; else &#123;</span><br><span class="hljs-regexp">    throw new Error();</span><br><span class="hljs-regexp">  &#125;</span><br><span class="hljs-regexp">&#125;</span><br></code></pre></td></tr></table></figure>
<ul>
<li>关键点： 用<code>dispatch</code>替代依赖(<strong><code>dispatch</code>会保证生命周期内保持不变</strong>)</li>
<li>这种模式的好处：通过<code>reducer</code>让我们不必再关心<code>state</code>和<code>props</code>的状态，成功达到了解耦的目的。</li>
</ul>
<p><em>但是问题来了，如果我每次都想获取最新的<code>props</code>，还有戏嘛？</em></p>
<p><strong>有的，把reducer扔组件里</strong></p>
<p>每次<code>dispatch</code>，都会调用<code>reducer</code>，这时候<code>reducer</code>获取的就是最新的<code>props</code>了。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Counter</span>(<span class="hljs-params">&#123; step &#125;</span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [count, dispatch] = useReducer(reducer, <span class="hljs-number">0</span>);<br><br>  <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">reducer</span>(<span class="hljs-params">state, action</span>) </span>&#123;<br>    <span class="hljs-keyword">if</span> (action.type === <span class="hljs-string">'tick'</span>) &#123;<br>      <span class="hljs-keyword">return</span> state + step;<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>      <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-built_in">Error</span>();<br>    &#125;<br>  &#125;<br><br>  useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    <span class="hljs-keyword">const</span> id = setInterval(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>      dispatch(&#123; <span class="hljs-attr">type</span>: <span class="hljs-string">'tick'</span> &#125;);<br>    &#125;, <span class="hljs-number">1000</span>);<br>    <span class="hljs-keyword">return</span> <span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> clearInterval(id);<br>  &#125;, [dispatch]);<br><br>  <span class="hljs-keyword">return</span> <span class="xml"><span class="hljs-tag">&lt;<span class="hljs-name">h1</span>&gt;</span>&#123;count&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">h1</span>&gt;</span></span>;<br>&#125;<br></code></pre></td></tr></table></figure>
<p><em>难怪Dan说是Hooks的作弊模式</em></p>
<p><strong>接下来，我们着重解决第三和第四个问题</strong></p>
<h2 id="Effect中的数据请求"><a href="#Effect中的数据请求" class="headerlink" title="Effect中的数据请求"></a>Effect中的数据请求</h2><hr>
<p><strong>直接上案例</strong></p>
<p>这个模式曾经也是我在不是很懂Effect的情况下经常使用的模式，我曾经对<code>eslint-plugin-react-hooks</code>这个插件提供的警告存有很大的疑问，现在终于明白了。</p>
<p>乍一看？没问题！</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">SearchResults</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-comment">// Imagine this function is long</span><br>  <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getFetchUrl</span>(<span class="hljs-params"></span>) </span>&#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-string">'https://hn.algolia.com/api/v1/search?query=react'</span>;<br>  &#125;<br><br>  <span class="hljs-comment">// Imagine this function is also long</span><br>  <span class="hljs-keyword">async</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">fetchData</span>(<span class="hljs-params"></span>) </span>&#123;<br>    <span class="hljs-keyword">const</span> result = <span class="hljs-keyword">await</span> axios(getFetchUrl());<br>    setData(result.data);<br>  &#125;<br><br>  useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    fetchData();<br>  &#125;, []);<br>&#125;<br></code></pre></td></tr></table></figure>
<ul>
<li>但是上述模式存在一个弊端，如果我们忘记写入依赖，那么我们的effects就不会同步props和state带来的变更。这当然不是我们想要的。</li>
</ul>
<p>我们把他放进去，前提是某些函数仅在effect中调用。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">SearchResults</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-comment">// ...</span><br>  useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    <span class="hljs-comment">// We moved these functions inside!</span><br>    <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getFetchUrl</span>(<span class="hljs-params"></span>) </span>&#123;<br>      <span class="hljs-keyword">return</span> <span class="hljs-string">'https://hn.algolia.com/api/v1/search?query=react'</span>;<br>    &#125;<br>    <span class="hljs-keyword">async</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">fetchData</span>(<span class="hljs-params"></span>) </span>&#123;<br>      <span class="hljs-keyword">const</span> result = <span class="hljs-keyword">await</span> axios(getFetchUrl());<br>      setData(result.data);<br>    &#125;<br><br>    fetchData();<br>  &#125;, []); <span class="hljs-comment">// ✅ Deps are OK</span><br>  <span class="hljs-comment">// ...</span><br>&#125;<br></code></pre></td></tr></table></figure>
<p>还是没问题吗？是的，没问题了。</p>
<p>稍微修改一下，使请求url中需要我们的状态，对，就是这么请求。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">SearchResults</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [query, setQuery] = useState(<span class="hljs-string">'react'</span>);<br><br><br>  useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    <span class="hljs-comment">// Imagine this function is also long</span><br>    <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">getFetchUrl</span>(<span class="hljs-params"></span>) </span>&#123;<br>      <span class="hljs-keyword">return</span> <span class="hljs-string">'https://hn.algolia.com/api/v1/search?query='</span> + query;<br>    &#125;<br><br>    <span class="hljs-comment">// Imagine this function is also long</span><br>    <span class="hljs-keyword">async</span> <span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">fetchData</span>(<span class="hljs-params"></span>) </span>&#123;<br>      <span class="hljs-keyword">const</span> result = <span class="hljs-keyword">await</span> axios(getFetchUrl());<br>      setData(result.data);<br>    &#125;<br>    fetchData();<br>  &#125;, [query]);<br>&#125;<br></code></pre></td></tr></table></figure>
<p>但我曾经有一次，在封装自定义Hook的时候，我将request提供给自定义hook，但是我却没法将我的state或者是props放进自定义hook，因为他们属于不同的js文件，这可怎么办？<br>说白了，就是逻辑复用咋搞。</p>
<h3 id="useCallback"><a href="#useCallback" class="headerlink" title="useCallback"></a>useCallback</h3><p>这个Hook很简单，缓存一个函数，只在函数本身需要改变的时候调用副作用</p>
<p>怎么知道这个函数是否需要改变，通过第二个参数</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">SearchResults</span>(<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-keyword">const</span> [query, setQuery] = useState(<span class="hljs-string">'react'</span>);<br>  <span class="hljs-keyword">const</span> getFetchUrl = useCallback(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-string">'https://hn.algolia.com/api/v1/search?query='</span> + query;<br>  &#125;, [query]);  <span class="hljs-comment">// ✅ Callback deps are OK</span><br><br>  useEffect(<span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>    <span class="hljs-keyword">const</span> url = getFetchUrl(<span class="hljs-string">'react'</span>);<br>    <span class="hljs-comment">// ... Fetch data and do something ...</span><br>  &#125;, [getFetchUrl]); <span class="hljs-comment">// ✅ Effect deps are OK</span><br>&#125;<br></code></pre></td></tr></table></figure>
<p>问题解决！</p>
<p><em>Dan对<code>class</code>模式和<code>hooks</code>模式的这种网络请求没法放入依赖的情况分别作了比较，当request作为<strong>请求向下传递</strong>的情况。这里不细说了，class模式本身不是数据流的一部分，因此他必须将不必要的<code>query</code>传下去才能再<code>componentDidUpdate</code>中发生响应，而Hooks就完美的解决了这个问题</em></p>
<p><em>在我看来，useCallback就是一个工具人，我是老板，你没法直接跟我说话，就跟我秘书说。我秘书会传达给我的。</em></p>
<p>OK！1、2、3、4、5、6问题全都解决了！</p>
<h1 id="总结-amp-致谢"><a href="#总结-amp-致谢" class="headerlink" title="总结&amp;致谢"></a>总结&amp;致谢</h1><p>前前后后通读了Dan的文章好多遍，并看了好几遍Effect的文档。初识Effect，似乎很简单，随着项目的锻炼，发现越来越难以管理自己的状态。花心思重新学了一下Effect。让我更清晰的明白了设计Hooks的初衷和目的，找到正确使用的姿势。</p>
<p>从阅读到完成，花了大概有半个月，主要参考资料来自Dan的Overreacted上的一篇文章useEffect完整指南。</p>
]]></content>
      <categories>
        <category>React</category>
      </categories>
      <tags>
        <tag>React</tag>
        <tag>前端</tag>
        <tag>Hooks</tag>
      </tags>
  </entry>
  <entry>
    <title>React运行时</title>
    <url>/2020/08/02/React-UI-render/</url>
    <content><![CDATA[<h1 id="React运行时"><a href="#React运行时" class="headerlink" title="React运行时"></a>React运行时</h1><blockquote>
<p>Dan在OverReact上发表的文章深入浅出，本文只针对个人之前不理解的点进行思考，采用了他的案例，参考了他的文章——<a href="https://overreacted.io/zh-hans/react-as-a-ui-runtime/" target="_blank" rel="noopener">将 React 作为 UI 运行时</a>。</p>
</blockquote>
<blockquote>
<p>再写UI运行时的时候,发现一切都离不开Fiber,这里参考了掘金博主<em>武器大师1024</em>的一篇文章,并补充了一些他的案例<a href="https://juejin.im/post/6844903582622285831" target="_blank" rel="noopener">React Fiber</a>,同时,此案例也是<a href="https://www.youtube.com/watch?v=ZCuYPiUIONs" target="_blank" rel="noopener">Lin Clark presentation in ReactConf 2017</a>演讲上的一篇案例。</p>
</blockquote>
<p>大多数人关注React，关注它的使用方法，关注它创建页面的逻辑和效果，而忽略其模型和React本质上的一些东西，到底React帮我们做了什么?</p>
<a id="more"></a>

<hr>
<h2 id="宿主树和宿主实例"><a href="#宿主树和宿主实例" class="headerlink" title="宿主树和宿主实例"></a>宿主树和宿主实例</h2><hr>
<h4 id="什么是宿主树，什么是宿主实例？"><a href="#什么是宿主树，什么是宿主实例？" class="headerlink" title="什么是宿主树，什么是宿主实例？"></a>什么是宿主树，什么是宿主实例？</h4><p>这是一个概念上的问题，区别两者，其实很简单，就像<strong>整体</strong>和<strong>局部</strong>的关系。例如一个Dom树，就是一个宿主树，Dom树中的一个<code>div</code>，就是一个宿主实例。</p>
<p>宿主实例有自己的属性和方法，我们可以操作宿主方法做一些想做的事情，例如<code>appendChild</code>，<code>setAttribute</code>等等</p>
<h4 id="React又是怎么利用宿主树和宿主实例的呢？"><a href="#React又是怎么利用宿主树和宿主实例的呢？" class="headerlink" title="React又是怎么利用宿主树和宿主实例的呢？"></a>React又是怎么利用宿主树和宿主实例的呢？</h4><p>在我看来，React的宿主树，是由其宿主实例构成的，而React的宿主实例，就是<strong>React元素</strong>，React元素是一个对象，与DOM树横向比对，大同小异，所以React再做UI渲染时，可以轻松的将其映射为对应的DOM元素，React经常帮我们做的，就是这个。</p>
<p>React解放了我们直接操作宿主树和宿主实例，因为React帮我们做了这个事情。做这些事情的同时，React又保证了另外两个事情:</p>
<ul>
<li><strong>稳定性</strong>: 宿主树的稳定性</li>
<li><strong>通用性</strong>：宿主树中的宿主实例单独抽出来，依旧不会改变其UI结构，达到可复用的状态</li>
</ul>
<h4 id="React操作宿主树和DOM有什么不同？"><a href="#React操作宿主树和DOM有什么不同？" class="headerlink" title="React操作宿主树和DOM有什么不同？"></a>React操作宿主树和DOM有什么不同？</h4><ul>
<li>DOM会导致突变，直接操作宿主实例。在之后往里面增加或者删除子节点。宿主实例是完全可变的。</li>
<li>React是通过克隆双亲树并始终替换掉顶级子树的宿主环境。</li>
</ul>
<h4 id="React的宿主实例——React元素"><a href="#React的宿主实例——React元素" class="headerlink" title="React的宿主实例——React元素"></a>React的宿主实例——React元素</h4><ul>
<li>React的元素不是永远存在的，他总是在<strong>重建</strong>和<strong>删除</strong>之间循环</li>
<li>你<strong>不能直接</strong>操作React元素(与DOM不同)，如果想改变，从头开始渲染</li>
</ul>
<h2 id="React渲染"><a href="#React渲染" class="headerlink" title="React渲染"></a>React渲染</h2><hr>
<h3 id="ReactDOM-render"><a href="#ReactDOM-render" class="headerlink" title="ReactDOM.render"></a>ReactDOM.render</h3><h4 id="初次调用"><a href="#初次调用" class="headerlink" title="初次调用"></a>初次调用</h4><p>看以下一段代码</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">ReactDOM.render(<br>  <span class="hljs-comment">// &#123; type: 'button', props: &#123; className: 'blue' &#125; &#125;</span><br>  &lt;button className=<span class="hljs-string">"blue"</span> /&gt;,<br>  <span class="hljs-built_in">document</span>.getElementById(<span class="hljs-string">'container'</span>)<br>);<br></code></pre></td></tr></table></figure>

<ul>
<li>初始调用时，实际操作就是把你的宿主树转换成实际的dom树渲染</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// 在 ReactDOM 渲染器内部（简化版）</span><br><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">createHostInstance</span>(<span class="hljs-params">reactElement</span>) </span>&#123;<br>  <span class="hljs-keyword">let</span> domNode = <span class="hljs-built_in">document</span>.createElement(reactElement.type); <span class="hljs-comment">// 这里是button</span><br>  domNode.className = reactElement.props.className; <span class="hljs-comment">// 这里是blue</span><br>  <span class="hljs-keyword">return</span> domNode;<br>&#125;<br><br><span class="hljs-comment">// 然后将其放进dom</span><br>domContainer.appendChild(domNode);<br></code></pre></td></tr></table></figure>

<h4 id="再次调用会如何做"><a href="#再次调用会如何做" class="headerlink" title="再次调用会如何做"></a>再次调用会如何做</h4><ul>
<li>想必看过React文档的都知道，会进行比对，只对需要更新的一部分进行更新，下面贴了Dan文章里的两段代码，简述了React的过程。但实际比对算法要复杂的多</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">ReactDOM.render(<br>  &lt;button className=<span class="hljs-string">"blue"</span> /&gt;,<br>  <span class="hljs-built_in">document</span>.getElementById(<span class="hljs-string">'container'</span>)<br>);<br><br><span class="hljs-comment">// ... 之后 ...</span><br><br><span class="hljs-comment">// 应该替换掉 button 宿主实例吗？</span><br><span class="hljs-comment">// 还是在已有的 button 上更新属性？</span><br>ReactDOM.render(<br>  &lt;button className=<span class="hljs-string">"red"</span> /&gt;,<br>  <span class="hljs-built_in">document</span>.getElementById(<span class="hljs-string">'container'</span>)<br>);<br></code></pre></td></tr></table></figure>

<ul>
<li>React工作流程大致如下</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// let domNode = document.createElement('button');</span><br><span class="hljs-comment">// domNode.className = 'blue';</span><br><span class="hljs-comment">// domContainer.appendChild(domNode);</span><br>ReactDOM.render(<br>  &lt;button className=<span class="hljs-string">"blue"</span> /&gt;,<br>  <span class="hljs-built_in">document</span>.getElementById(<span class="hljs-string">'container'</span>)<br>);<br><br><span class="hljs-comment">// 能重用宿主实例吗？能！(button → button)</span><br><span class="hljs-comment">// domNode.className = 'red';</span><br>ReactDOM.render(<br>  &lt;button className=<span class="hljs-string">"red"</span> /&gt;,<br>  <span class="hljs-built_in">document</span>.getElementById(<span class="hljs-string">'container'</span>)<br>);<br><br><span class="hljs-comment">// 能重用宿主实例吗？不能！(button → p)</span><br><span class="hljs-comment">// domContainer.removeChild(domNode);</span><br><span class="hljs-comment">// domNode = document.createElement('p');</span><br><span class="hljs-comment">// domNode.textContent = 'Hello';</span><br><span class="hljs-comment">// domContainer.appendChild(domNode);</span><br>ReactDOM.render(<br>  &lt;p&gt;Hello&lt;<span class="hljs-regexp">/p&gt;,</span><br><span class="hljs-regexp">  document.getElementById('container')</span><br><span class="hljs-regexp">);</span><br><span class="hljs-regexp"></span><br><span class="hljs-regexp">/</span><span class="hljs-regexp">/ 能重用宿主实例吗？能！(p → p)</span><br><span class="hljs-regexp">/</span><span class="hljs-regexp">/ domNode.textContent = 'Goodbye';</span><br><span class="hljs-regexp">ReactDOM.render(</span><br><span class="hljs-regexp">  &lt;p&gt;Goodbye&lt;/</span>p&gt;,<br>  <span class="hljs-built_in">document</span>.getElementById(<span class="hljs-string">'container'</span>)<br>);<br></code></pre></td></tr></table></figure>

<p>这里有个小注意点需要注意一下，当遇到条件语句时，React并不会重新创建元素，这依赖于元素的顺序，例如：这里的input始终在第二个</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Form</span>(<span class="hljs-params">&#123; showMessage &#125;</span>) </span>&#123;<br>  <span class="hljs-keyword">let</span> message = <span class="hljs-literal">null</span>;<br>  <span class="hljs-keyword">if</span> (showMessage) &#123;<br>    message = &#123;<br>      type: <span class="hljs-string">'p'</span>,<br>      props: &#123; <span class="hljs-attr">children</span>: <span class="hljs-string">'I was just added here!'</span> &#125;<br>    &#125;;<br>  &#125;<br>  <span class="hljs-keyword">return</span> &#123;<br>    type: <span class="hljs-string">'dialog'</span>,<br>    props: &#123;<br>      children: [<br>        message,<br>        &#123; <span class="hljs-attr">type</span>: <span class="hljs-string">'input'</span>, <span class="hljs-attr">props</span>: &#123;&#125; &#125;<br>      ]<br>    &#125;<br>  &#125;;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>由上面的小注意点引申出，当遇到列表时，React会去比对元素，如果<strong>元素相同</strong>，那么<strong>直接复用</strong>。问题是，当我们遇到类似于商品列表时，商品列表往往各项都不相同，或者说一个列表中有几项相同，那么React该怎么办，如果还是按照顺序来，那每个元素都将被更新。</p>
<p>这样导致的问题是，每个元素更新，消耗性能。同时，React会把类似于input这样的组件当时的值一并复用过来，导致潜在的bug。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">ShoppingList</span>(<span class="hljs-params">&#123; list &#125;</span>) </span>&#123;<br>  <span class="hljs-keyword">return</span> (<br>    &lt;form&gt;<br>      &#123;list.map(<span class="hljs-function"><span class="hljs-params">item</span> =&gt;</span> (<br>        &lt;p&gt;<br>          You bought &#123;item.name&#125;<br>          &lt;br /&gt;<br>          Enter how many <span class="hljs-keyword">do</span> you want: <span class="xml"><span class="hljs-tag">&lt;<span class="hljs-name">input</span> /&gt;</span></span><br>        &lt;<span class="hljs-regexp">/p&gt;</span><br><span class="hljs-regexp">      ))&#125;</span><br><span class="hljs-regexp">    &lt;/</span>form&gt;<br>  )<br>&#125;<br></code></pre></td></tr></table></figure>

<p>React将会以以下的形式更新：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">for</span> (<span class="hljs-keyword">let</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++) &#123;<br>  <span class="hljs-keyword">let</span> pNode = formNode.childNodes[i];<br>  <span class="hljs-keyword">let</span> textNode = pNode.firstChild;<br>  textNode.textContent = <span class="hljs-string">'You bought '</span> + items[i].name;<br>&#125;<br></code></pre></td></tr></table></figure>

<ul>
<li>因此我们需要一个唯一标识key，来告诉React渲染前后元素是否是相同的。</li>
</ul>
<p><em>组件和元素大同小异，React也会递归组件，渲染想要的元素。组件首字母必须大写，为了让React区分，告诉它这是一个组件而不是一个dom元素</em></p>
<p><em>补充两个小点</em></p>
<ul>
<li><p>什么是Hooks？</p>
<ul>
<li>其实Hooks是绑定组件局部状态的特性。</li>
</ul>
</li>
<li><p>什么是局部状态</p>
<ul>
<li>局部状态是宿主实例相关的能拥有的状态，例如focus、selection等等。</li>
</ul>
</li>
</ul>
<h3 id="控制反转"><a href="#控制反转" class="headerlink" title="控制反转"></a>控制反转</h3><p>组件是函数，为什么不用函数调用，而用JSX的形式呢？</p>
<ul>
<li>我们自身调用函数，控制权在我们手里，用JSX的形式，React会为我们调用函数，这就是控制反转。使用控制反转有以下几个好处：<ul>
<li><strong>组件 &gt; 函数</strong> React组件帮助我们维护了局部状态</li>
<li><strong>组件参与协调</strong> 也就是说React调用组件，可以更了解宿主树的结构，便于进行比对</li>
<li><strong>组件推迟协调</strong> 不会引起渲染导致的主线程阻塞</li>
<li><strong>组件便于调试</strong> React提供了丰富的组件调试工具</li>
<li><strong>组件可以懒加载</strong> 组件不会去执行不需要执行的东西，例如<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-function"><span class="hljs-keyword">function</span> <span class="hljs-title">Page</span>(<span class="hljs-params">&#123; currentUser, children &#125;</span>) </span>&#123;<br>    <span class="hljs-keyword">if</span> (!currentUser.isLoggedIn) &#123;<br>        <span class="hljs-keyword">return</span> <span class="xml"><span class="hljs-tag">&lt;<span class="hljs-name">h1</span>&gt;</span>Please login<span class="hljs-tag">&lt;/<span class="hljs-name">h1</span>&gt;</span></span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> (<br>        &lt;Layout&gt;<br>            &#123;children&#125;<br>        &lt;<span class="hljs-regexp">/Layout&gt;</span><br><span class="hljs-regexp">    );</span><br><span class="hljs-regexp">&#125;</span><br><span class="hljs-regexp"></span><br><span class="hljs-regexp">/</span><span class="hljs-regexp">/ 这里有个地方调用组件</span><br><span class="hljs-regexp">&lt;Page&gt;</span><br><span class="hljs-regexp">    &#123;Comments()&#125; /</span><span class="hljs-regexp">/ 这个Comments函数无论是否登录永远都将执行</span><br><span class="hljs-regexp">&lt;/</span>Page&gt;<br><br><span class="hljs-comment">// 这里有个地方调用组件</span><br>&lt;Page&gt;<br>    &lt;Comments /&gt; <span class="hljs-comment">// 这个Comments组件</span><br>&lt;<span class="hljs-regexp">/Page&gt;</span><br></code></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h3 id="渲染阶段-React-Fiber-和提交阶段"><a href="#渲染阶段-React-Fiber-和提交阶段" class="headerlink" title="渲染阶段(React Fiber)和提交阶段"></a>渲染阶段(<code>React Fiber</code>)和提交阶段</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ClickCounter</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">React</span>.<span class="hljs-title">Component</span> </span>&#123;<br>    <span class="hljs-keyword">constructor</span>(props) &#123;<br>        <span class="hljs-keyword">super</span>(props);<br>        <span class="hljs-keyword">this</span>.state = &#123;<span class="hljs-attr">count</span>: <span class="hljs-number">0</span>&#125;;<br>        <span class="hljs-keyword">this</span>.handleClick = <span class="hljs-keyword">this</span>.handleClick.bind(<span class="hljs-keyword">this</span>);<br>    &#125;<br><br>    handleClick() &#123;<br>        <span class="hljs-keyword">this</span>.setState(<span class="hljs-function">(<span class="hljs-params">state</span>) =&gt;</span> &#123;<br>            <span class="hljs-keyword">return</span> &#123; <span class="hljs-attr">count</span>: state.count + <span class="hljs-number">1</span> &#125;;<br>        &#125;);<br>    &#125;<br>    <br>    componentDidUpdate() &#123;&#125;<br><br>    render() &#123;<br>        <span class="hljs-keyword">return</span> [<br>            &lt;button key=<span class="hljs-string">"1"</span> onClick=&#123;<span class="hljs-keyword">this</span>.handleClick&#125;&gt;Update counter&lt;<span class="hljs-regexp">/button&gt;,</span><br><span class="hljs-regexp">            &lt;span key="2"&gt;&#123;this.state.count&#125;&lt;/</span>span&gt;<br>        ]<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<h4 id="React-Fiber"><a href="#React-Fiber" class="headerlink" title="React Fiber"></a>React Fiber</h4><p>什么是Fiber？为什么使用Fiber？看以下的图:</p>
<p><img src="stack-reconciler" alt=""></p>
<p><img src="Fiber-reconciler" alt=""></p>
<p><code>React 16</code>版本之前，<code>React</code>完全通过<code>stack reconciler</code>进行比对(见图1)，这种比对会在<code>setState</code>进行后立即发生，会递归函数，当<code>vDom</code>遍历完成之后，得到对应的真实Dom的信息，并传递给<code>render</code>进行渲染。如果有大量的比对，那么会<strong>消耗过长的时间</strong>，浏览器线程此时被js代码所<strong>阻塞</strong>，如果遇到类似于动画效果，那么动画效果将被延迟执行，不仅如此，交互布局都将被阻塞，给用户的感觉就是卡顿的感觉。</p>
<p><img src="stack-workflow" alt=""></p>
<p>上述图片展示了<code>stack</code>工作时的内存情况，因此，<code>React</code>重写了其算法，使用了<code>Fiber</code>。</p>
<h4 id="Schedule-调度"><a href="#Schedule-调度" class="headerlink" title="Schedule(调度)"></a>Schedule(调度)</h4><p>调度是什么？—— 调度就是调动，安排人力，车辆。(来自百度百科)</p>
<p>我们已经知道<code>stack reconciler</code>模式的弊端，比对占用大量时间，阻塞了主线程。因此，我们需要有人安排，何时能够进行比对。这种调度是<code>Fiber reconciler</code>中的过程之一，如果浏览器主线程的优先级高，那么我们优先进行浏览器想要进行的操作。类似于下图。</p>
<p><img src="Fiber-workflow" alt=""></p>
<p>如上图所示，我们需要达成此目的，我们需要拆分我们的宿主树，拆分成一个个<strong>片段</strong>，以链表的形式，为每个节点添加一些必要的数据结构，来达成连续的片段。并且这些连续的片段可以被高优先级的任务随时中断。</p>
<p>这样引发一个问题，真实<code>Dom</code>与我当前的<code>vDom</code>不一致(由于我可能已经更新了一些片段)，因此，调度过程中，我们必须保存我们的调度信息，除了当前正在比对的树(current树)。我们还需要维护一棵正在比对的树，这棵树保存的是已经比对过的片段(workInProgress树)。</p>
<h5 id="怎么和浏览器进行通信，何时和浏览器进行通信呢"><a href="#怎么和浏览器进行通信，何时和浏览器进行通信呢" class="headerlink" title="怎么和浏览器进行通信，何时和浏览器进行通信呢?"></a>怎么和浏览器进行通信，何时和浏览器进行通信呢?</h5><p>客户端执行任务按帧来计算，通常执行帧在30~60帧之间。在两个执行帧之间的空闲时间，通常被用来进行调度，执行任务。</p>
<p><img src="requestIdelCallback" alt=""></p>
<p>如上图所示，浏览器可以在这两个空闲期之间执行<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/requestIdleCallback" target="_blank" rel="noopener"><code>requestIdleCallback</code></a>，浏览器同时也会传递给回调函数<code>deadline</code>相关的对象信息，如果说你这个<code>fiber node</code>执行时间过长，这个<code>fiber node</code>依旧会执行完才会去向浏览器征求意见，如果说你这个里面写了个死循环，那么页面将会卡死，通常一个<code>fiber node</code>不会超过16ms，超过了则将出现卡顿的情况。</p>
<h4 id="一个例子"><a href="#一个例子" class="headerlink" title="一个例子"></a>一个例子</h4><blockquote>
<p>这个例子借用React Conf 2017上Lin Clark的演讲(<a href="https://www.youtube.com/watch?v=ZCuYPiUIONs&list=PLb0IAmt7-GS3fZ46IGFirdqKTIxlws7e0&index=6&t=0s" target="_blank" rel="noopener">附上链接</a>)</p>
</blockquote>
<p>现在有一个列表，渲染一个<code>button</code>和一组<code>item</code>, <code>item</code>中包含一个<code>div</code>，其中的内容为数字。通过点击<code>button</code>，可以使列表中的所有数字进行平方。另外有一个按钮，点击可以调节字体大小。</p>
<p><img src="demo-0" alt=""></p>
<p>1.0 页面初始化完成，渲染一个<code>fiber tree</code>，如下图所示</p>
<p><em>这里注意：每个<code>fiber node</code>，<code>child</code>指针指向的节点都为其直接子节点，至于其他子节点，将会由直接子节点的<code>sibling</code>指针指向，具体可见<code>fiber</code>数据结构，每个<code>fiber node</code>都向其父节点返回</em></p>
<p><img src="demo-0.1" alt=""></p>
<p>1.0.1 React同时维护用于计算更新，保存当前计算状态的树，称之为<code>workInProgressTree</code>，。</p>
<p><img src="demo-0.2" alt=""></p>
<p>1.1 用户点击平方操作，调用<code>setState</code>，<code>React</code>将其送入更新队列(<code>update queue</code>), 不会立即进行对比，修改<code>dom</code>，而是交给调度器(<code>scheduler</code>)</p>
<p><img src="demo-0.3" alt=""></p>
<p>1.2 调度器会去询问主线程的使用情况，上文已经描述了如何与浏览器进行通信，借用<code>requestIdelCallback</code>，根据优先级执行任务，低优先级通过<code>requestIdelCallback</code>，高优先级(动画)通过<code>requestAnimationFrame</code>执行。</p>
<p><img src="requestIdelCallback" alt=""></p>
<p>2.1 一旦调度器得到了时间，就开始进入了<code>work loop</code>，这种循环是等待与执行的循环，每次<code>fiber node</code><strong>执行完成</strong>才会去询问浏览器主线程状态，上文说了，通常是在16ms之内完成，否则会有卡顿情况，如果写了死循环，依旧会导致页面卡死的情况。在这个过程中，当前的<code>fiber node</code>需要记住下一个工作单元，如果浏览器有时间，那么我将进行下一个<code>fiber node</code>的任务执行</p>
<p><img src="demo-1" alt=""></p>
<p>2.2 根节点上的更新队列为空，所以直接从<code>fiber-tree</code>上将根节点复制到<code>workInProgressTree</code>中去。根节点中包含指向子节点<code>List</code>的指针。</p>
<p><img src="demo-2" alt=""></p>
<p>2.3 根节点根据child指针，将<code>List</code>节点和对应的更新队列一起放到<code>workInProgressTree</code>中，<code>List</code>插入后返回给父节点，标志着根节点<code>fiber node</code>处理完成</p>
<p><img src="demo-3" alt=""></p>
<p>2.4 重复1.2操作，询问主线程情况</p>
<p><img src="demo-4" alt=""></p>
<p>3.1 处理下一个<code>List</code>节点，进入<code>work loop</code>，因此此时<code>react</code>会调用<code>setState</code>时传入的<code>updater funciton</code>获取最新的state值，此时应该是<code>[1,4,9]</code></p>
<ul>
<li>在继续之前，我们简单回顾一下<code>setState</code>的前几步流程:<ul>
<li>触发<code>setState</code>函数，将触发<code>setState</code>的<code>this</code>和<code>setState</code>的参数传入<code>enqueueSetState</code>函数中。</li>
<li><code>enqueueSetState</code>函数，提出当前触发<code>setState</code>的<code>Fiber</code>节点并将传入的<code>setState</code>的参数创建一个<code>update</code>对象，<code>update</code>对象中的<code>payload</code>就是传入的<code>state</code>对象。</li>
<li><code>enqueueUpdate</code>函数，将当前<code>Fiber</code>的<code>state</code>和需要修改的<code>state</code>创建一个对象传入当前<code>Fiber</code>节点的<code>updateQueue</code>对象中。<code>updateQueue</code>对象有几个关键值，<code>baseState</code>(当前<code>Fiber</code>节点的<code>state</code>)、<code>firstUpdate</code>(首个更新任务)、<code>lastUpdate</code>(最后一个更新任务，防止多次重复<code>setState</code>)。</li>
</ul>
</li>
</ul>
<p>在获取到最新的<code>state</code>值后，<code>react</code>会更新<code>List</code>的<code>state</code>和<code>props</code>值，并且根据<code>element</code>类型判断是否可重用，然后渲染到<code>workInProgress</code>树中，由于<code>List</code>组件有更新，因此我们需要将<code>List</code>打上标签，表明需要更新。</p>
<p><img src="demo-5" alt=""></p>
<p>3.2 重复1.2操作，询问主线程情况</p>
<p><img src="demo-6" alt=""></p>
<p>3.3 <code>button</code>没有子节点，也没有更新，因此直接标记<code>button</code>处理完成。</p>
<p><img src="demo-7" alt=""></p>
<p>3.4 重复1.2操作，询问主线程情况。由于我们点击了放大字体的操作，主线程已经有放大字体的操作在等待<code>fiber node</code>任务完成。</p>
<p><img src="demo-8" alt=""></p>
<p>3.5 对于每一个<code>item</code>,判断<code>shouldComponentUpdate</code>,如果不需要更新，那么我们像3.3中的<code>button</code>一样操作，如果需要更新，那么我们像3.1中的<code>List</code>一样操作，打需要更新的标签</p>
<p><img src="demo-9" alt=""></p>
<p>4.1 对于叶子节点<code>div</code>，无兄弟节点，更新操作也已结束，需要合并到父节点，才算<code>fiber node</code>完成。</p>
<p><img src="demo-10" alt=""></p>
<p>4.2 合并回到父节点，并移除标签，标记完成。</p>
<p><img src="demo-11" alt=""></p>
<p>4.3 重复1.2操作，询问主线程情况。现在有放大字体的操作在等待。因此交换主线程，放大字体。</p>
<p><img src="demo-12" alt=""></p>
<p>4.4 放大字体完成，<code>React</code>完成接下来的操作，形成一个完成的<code>workInProgress</code>树。</p>
<p><img src="demo-13" alt=""></p>
<p>4.5 那些更新的<code>item</code>向<code>List</code>返回, 标签列表如下</p>
<p><img src="demo-14" alt=""></p>
<p>4.6 所有节点标记完成后,<code>React</code>正式进入提交阶段,同样询问主线程,时间够用,更新<code>Dom</code></p>
<p><img src="demo-15" alt=""></p>
<p>4.7 更新完过后还需要交换指针</p>
<p><img src="demo-16" alt=""></p>
<h4 id="废除生命周期"><a href="#废除生命周期" class="headerlink" title="废除生命周期"></a>废除生命周期</h4><ul>
<li>为什么<code>Fiber Reconciler</code>下, <code>React</code>要废除生命周期例如<code>componentWillUpdate</code>?</li>
</ul>
<p>我们通过刚刚<code>Fiber Reconciler</code>的过程我们看到, 这个过程是可以被高优先级例如动画所打断的, 我们初次调用<code>componentWillUpdate</code>, 结果我<code>Reconciler</code>到一半,被打断了, 当主线程又有时间的时候,我又要进行<code>componentWillUpdate</code>,显然是不合理的.</p>
]]></content>
      <categories>
        <category>React</category>
      </categories>
      <tags>
        <tag>React</tag>
        <tag>前端</tag>
      </tags>
  </entry>
  <entry>
    <title>Redux</title>
    <url>/2020/08/02/Redux/</url>
    <content><![CDATA[<h1 id="Redux"><a href="#Redux" class="headerlink" title="Redux"></a>Redux</h1><p>本文默认会使用基本的Redux, 主要对自己学习Redux中一些比较困难的点进行思考和学习.</p>
<h2 id="Redux阅读和使用上遇到的一些难点"><a href="#Redux阅读和使用上遇到的一些难点" class="headerlink" title="Redux阅读和使用上遇到的一些难点"></a>Redux阅读和使用上遇到的一些难点</h2><h3 id="reducer"><a href="#reducer" class="headerlink" title="reducer"></a>reducer</h3><h2 id="实现一个简易的Redux"><a href="#实现一个简易的Redux" class="headerlink" title="实现一个简易的Redux"></a>实现一个简易的Redux</h2><h3 id="createStore"><a href="#createStore" class="headerlink" title="createStore"></a>createStore</h3><h3 id="Provider"><a href="#Provider" class="headerlink" title="Provider"></a>Provider</h3><h3 id="connect"><a href="#connect" class="headerlink" title="connect"></a>connect</h3><h3 id="middleWare"><a href="#middleWare" class="headerlink" title="middleWare"></a>middleWare</h3><h2 id="React-Redux-Hooks"><a href="#React-Redux-Hooks" class="headerlink" title="React-Redux Hooks"></a>React-Redux Hooks</h2><h3 id="useStore"><a href="#useStore" class="headerlink" title="useStore"></a>useStore</h3><h3 id="useSelect"><a href="#useSelect" class="headerlink" title="useSelect"></a>useSelect</h3><h3 id="useDispatch"><a href="#useDispatch" class="headerlink" title="useDispatch"></a>useDispatch</h3>]]></content>
      <categories>
        <category>React</category>
      </categories>
      <tags>
        <tag>React</tag>
        <tag>前端</tag>
        <tag>Redux</tag>
      </tags>
  </entry>
  <entry>
    <title>webpack基本配置</title>
    <url>/2020/11/29/webpack/</url>
    <content><![CDATA[<h1 id="webpack基本配置"><a href="#webpack基本配置" class="headerlink" title="webpack基本配置"></a>webpack基本配置</h1><h2 id="基本搭建"><a href="#基本搭建" class="headerlink" title="基本搭建"></a>基本搭建</h2><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">npm init -y<br></code></pre></td></tr></table></figure>
<p>一顿yes直接搭建好基本的node环境<br><img src="%E5%88%9D%E5%A7%8B%E5%8C%96.jpg" alt=""></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">yarn install webpack webpack-cli --save-dev<br></code></pre></td></tr></table></figure>
<p>安装webpack, webpack-cli, 由于是开发环境需要,加上–save-dev</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>    entry: <span class="hljs-string">"./index.js"</span>,<br>    mode: <span class="hljs-string">"development"</span>,<br>    output: &#123;<br>        path: path.resolve(__dirname, <span class="hljs-string">'dist'</span>),<br>    &#125;,<br>&#125;<br></code></pre></td></tr></table></figure>
<p>新建<code>index.js</code>, 新建<code>webpack.config.js</code>, <code>webpack</code>默认读取<code>webpack.config.js</code>导出的对象.</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">npx webpack<br></code></pre></td></tr></table></figure>
<p><code>webpack</code>默认读取根路径下的<code>webpack.config.js</code>文件, <code>npm</code>5.2之后提供了自动安装<code>npx</code> ,会自动查找当前依赖包中的可执行文件并执行<br><img src="%E6%88%90%E5%8A%9F%E6%89%A7%E8%A1%8C.jpg" alt=""></p>
<a id="more"></a>

<hr>
<h2 id="context"><a href="#context" class="headerlink" title="context"></a>context</h2><p>定义了打包的基础目录, 一旦设置, 那么接下来配置设置的根路径将会是配置的基础目录。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> path = <span class="hljs-built_in">require</span>(<span class="hljs-string">'path'</span>);<br><br><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  context: path.resolve(__dirname, <span class="hljs-string">'app'</span>)<br>&#125;;<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="entry"><a href="#entry" class="headerlink" title="entry"></a>entry</h2><p>文件打包的起点</p>
<ul>
<li>可以传入数组, 传入数组从前往后依次进行打包, 包名为main</li>
<li>可以传入对象, 每个键作为打包的包名， 值为打包的资源路径或者打包的描述符<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  entry: <span class="hljs-string">'./index.js'</span><br>&#125;;<br><br><span class="hljs-comment">// 也可以自定义包名</span><br><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  entry: &#123;<br>    app: <span class="hljs-string">'./app.js'</span>,<br>    home: &#123; <span class="hljs-attr">import</span>: <span class="hljs-string">'./contact.js'</span>, <span class="hljs-attr">filename</span>: <span class="hljs-string">'pages/[name][ext]'</span> &#125;,<br>    about: &#123; <span class="hljs-attr">import</span>: <span class="hljs-string">'./about.js'</span>, <span class="hljs-attr">filename</span>: <span class="hljs-string">'pages/[name][ext]'</span> &#125;<br>  &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>
多入口可以设置打包依赖， 减少包体积， 避免重复使用相同的包<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  entry: &#123;<br>    app: &#123; <span class="hljs-attr">import</span>: [<span class="hljs-string">'./app.js'</span>, <span class="hljs-string">'./app2.js'</span>], <span class="hljs-attr">dependOn</span>: <span class="hljs-string">'react-vendors'</span> &#125;,<br>    <span class="hljs-string">'react-vendors'</span>: [<span class="hljs-string">'react'</span>, <span class="hljs-string">'react-dom'</span>, <span class="hljs-string">'prop-types'</span>]<br>  &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>
可以动态引入， 将在make事件中触发<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  entry: <span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> <span class="hljs-string">'./demo'</span> <span class="hljs-comment">// () =&gt; new Promise((resolve) =&gt; resolve(['./demo', './demo2']))</span><br>&#125;;<br></code></pre></td></tr></table></figure>

</li>
</ul>
<hr>
<h2 id="mode"><a href="#mode" class="headerlink" title="mode"></a>mode</h2><p>打包模式分为<code>development</code>和<code>production</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  mode: <span class="hljs-string">'development'</span> <span class="hljs-comment">// 'production'</span><br>&#125;;<br></code></pre></td></tr></table></figure>
<p>可以在打包时决定</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs plain">npx webpack --mode&#x3D;development<br></code></pre></td></tr></table></figure>

<p>如果我们需要在不同模式下进行定义不同的打包行为， 我们可以定义为函数， 使用方法如下。</p>
<p><strong>开发环境打包文件<code>webpack.base.js</code></strong></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = <span class="hljs-function">(<span class="hljs-params">options</span>) =&gt;</span> (&#123;<br>    <span class="hljs-comment">// 这里注入基本配置</span><br>&#125;)<br></code></pre></td></tr></table></figure>
<p><strong>主打包文件<code>webpack.dev.js</code></strong></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = <span class="hljs-built_in">require</span>(<span class="hljs-string">'./webpack.base.js'</span>)(&#123;<br>    <span class="hljs-comment">// 这里将开发环境打包需要的配置注入</span><br>&#125;)<br></code></pre></td></tr></table></figure>
<p>通过配置进行打包， 选择对应的打包文件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">webpack --config webpack.dev.js<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="output"><a href="#output" class="headerlink" title="output"></a>output</h2><p>打包的输出形态， 包括bundle和静态文件</p>
<h3 id="filename"><a href="#filename" class="headerlink" title="filename"></a>filename</h3><p><code>filename</code> 是一个很常见的配置，就是对应于 <code>entry</code> 里面的输入文件。</p>
<p>这里可以使用可替换模板字符串的形式定义输出的打包文件名，浏览器存在缓存机制，如果名称相同，浏览器默认文件没有改变，这样打包文件名相同会使得获取文件出现问题，因此我们通过可替换模板字符串的形式定义输出的打包文件名。</p>
<ul>
<li>编译层<ul>
<li>name： chunk文件名</li>
</ul>
</li>
<li>chunk层<ul>
<li>id: 内部chunk的id</li>
<li>chunkhash：这个chunk的hash值</li>
</ul>
</li>
<li>模块层<ul>
<li>contenthash： 根据内容生成的hash</li>
<li>id：模块id</li>
<li>hash：模块的hash值</li>
</ul>
</li>
<li>文件层：<ul>
<li>file： 文件名称和路径</li>
<li>query： 带前缀 ? 的 query</li>
</ul>
</li>
<li>url层：<ul>
<li>url</li>
</ul>
</li>
</ul>
<blockquote>
<p>具体参照<a href="https://webpack.docschina.org/configuration/output/#outputfilename" target="_blank" rel="noopener">模板字符串替换</a></p>
</blockquote>
<h3 id="chunkFilename"><a href="#chunkFilename" class="headerlink" title="chunkFilename"></a>chunkFilename</h3><p><code>chunkFilename</code> 指未被列在 <code>entry</code> 中，却又需要被打包出来的 <code>chunk</code> 文件的名称。一般来说，这个 <code>chunk</code> 文件指的就是要懒加载的代码。</p>
<p>注意点： 默认文件输出为<code>[id].js</code>或者遵循<code>output.filename</code>的设置，如果<code>output.filename</code>设置了<code>name</code>， 则会被预先替换为<code>id</code></p>
<blockquote>
<p>可以参照这篇文章看到<a href="https://www.cnblogs.com/skychx/p/webpack-filename-chunkFilename.html" target="_blank" rel="noopener">filename和chunkFilename的区别</a></p>
</blockquote>
<h3 id="path"><a href="#path" class="headerlink" title="path"></a>path</h3><p>打包文件路径</p>
<h3 id="publicPath"><a href="#publicPath" class="headerlink" title="publicPath"></a>publicPath</h3><p>外部资源路径，通常以<code>/</code>结尾, 默认参照路径为HTML页面所在的路径为准</p>
<p>例如如下配置，请求一个<code>chunk</code>就相当于请求<code>/assets/1.chunk.js</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  output: &#123;<br>    publicPath: <span class="hljs-string">'/assets/'</span>,<br>    chunkFilename: <span class="hljs-string">'[id].chunk.js'</span><br>  &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<h3 id="library"><a href="#library" class="headerlink" title="library"></a>library</h3><p>组织打包的模块，库开发使用</p>
<h3 id="libraryTarget"><a href="#libraryTarget" class="headerlink" title="libraryTarget"></a>libraryTarget</h3><p>库打包引入的方式</p>
<hr>
<h2 id="Module"><a href="#Module" class="headerlink" title="Module"></a>Module</h2><p><code>module</code>对象用于模块的处理方式</p>
<h3 id="rules"><a href="#rules" class="headerlink" title="rules"></a>rules</h3><h4 id="对应值为-Rule-数组"><a href="#对应值为-Rule-数组" class="headerlink" title="对应值为[Rule]数组"></a>对应值为<code>[Rule]</code>数组</h4><p><code>rules</code>作用在于提供模块的创建规则，提供对应的<code>loader</code>和解析器(<code>parser</code>)</p>
<h4 id="Rule"><a href="#Rule" class="headerlink" title="Rule"></a>Rule</h4><p><code>Rule</code>是一个对象，这个对象分为三部分</p>
<ul>
<li>条件匹配： 条件匹配通过 <code>test</code> 、 <code>include</code> 、 <code>exclude</code> 和 <code>resource</code> 对资源进行匹配(这个资源指的是请求的资源)，并且属性 <code>issuer</code> 对 <code>issuer</code> 匹配, 这个 <code>issuer</code> 指的是请求者的文件绝对路径。是导入时的位置。</li>
<li>应用规则： 对选中的文件使用 <code>use</code> 来应用 <code>loader</code>，按照从后往前的顺序应用， 并可以为loader传递参数。</li>
<li>嵌套规则：父规则(文档这里没怎么理解, 在我看来是指一般的规则)、 <code>rule</code>、<code>oneOf</code>的顺序，可以通过<code>enforce</code>强制修改顺序。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  <span class="hljs-built_in">module</span>: &#123;<br>    rules: [ <span class="hljs-comment">// 以下对象对应一个Rule</span><br>      &#123;<br>        test: <span class="hljs-regexp">/\\.css$/</span>, <span class="hljs-comment">// loader1</span><br>      &#125;,<br>      &#123;<br>          test: <span class="hljs-regexp">/\\.js$/</span>, <span class="hljs-comment">// loader2</span><br>      &#125;<br>    ]<br>  &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<ul>
<li>注意： <code>loader</code> 顺序从右往左，从下往上执行，按照 <code>pitching</code> 顺序执行<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">|- a-loader <span class="hljs-string">`pitch`</span><br>  |- b-loader <span class="hljs-string">`pitch`</span><br>    |- c-loader <span class="hljs-string">`pitch`</span><br>      |- requested <span class="hljs-built_in">module</span> is picked up <span class="hljs-keyword">as</span> a dependency<br>    |- c-loader normal execution<br>  |- b-loader normal execution<br>|- a-loader normal execution<br></code></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="Rule-oneOf"><a href="#Rule-oneOf" class="headerlink" title="Rule.oneOf"></a>Rule.oneOf</h4><p>一旦匹配，就使用</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  <span class="hljs-built_in">module</span>: &#123;<br>    rules: [ <span class="hljs-comment">// 以下对象对应一个Rule</span><br>      &#123;<br>        test: <span class="hljs-regexp">/\\.css$/</span>,<br>        oneOf: [<br>            &#123;<br>                resourceQuery: <span class="hljs-regexp">/inline/</span>, <span class="hljs-comment">// foo.css?inline</span><br>                use: <span class="hljs-string">'url-loader'</span><br>            &#125;,<br>            &#123;<br>                resourceQuery: <span class="hljs-regexp">/external/</span>, <span class="hljs-comment">// foo.css?external</span><br>                use: <span class="hljs-string">'file-loader'</span><br>            &#125;<br>        ]<br>      &#125;<br>    ]<br>  &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<h4 id="Rule-use"><a href="#Rule-use" class="headerlink" title="Rule.use"></a>Rule.use</h4><p>提供一个 <code>loader</code> 数组, 从右往左进行匹配</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  <span class="hljs-built_in">module</span>: &#123;<br>    rules: [<br>      &#123;<br>        <span class="hljs-comment">//...</span><br>        use: [<br>          <span class="hljs-string">'style-loader'</span>,<br>          &#123;<br>            loader: <span class="hljs-string">'css-loader'</span>,<br>            options: &#123;<br>              importLoaders: <span class="hljs-number">1</span><br>            &#125;<br>          &#125;,<br>          &#123;<br>            loader: <span class="hljs-string">'less-loader'</span>,<br>            options: &#123;<br>              noIeCompat: <span class="hljs-literal">true</span><br>            &#125;<br>          &#125;<br>        ]<br>      &#125;<br>    ]<br>  &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<h4 id="Rule-exclude-include"><a href="#Rule-exclude-include" class="headerlink" title="Rule.exclude/include"></a>Rule.exclude/include</h4><p>排除/包括 <code>loader</code> 加载某个模块</p>
<h4 id="Rule-parse"><a href="#Rule-parse" class="headerlink" title="Rule.parse"></a>Rule.parse</h4><p>文件被 <code>loader</code> 执行后会转成一段js字符串， <code>parse</code>会将其转换为 <code>AST</code> 语法树，有了语法树就可以对代码为所欲为了，其中最重要的功能就是分析出这段代码依赖了哪些模块</p>
<h4 id="Rule-sideEffect"><a href="#Rule-sideEffect" class="headerlink" title="Rule.sideEffect"></a>Rule.sideEffect</h4><p>模块是否包含副作用</p>
<h4 id="Rule-type"><a href="#Rule-type" class="headerlink" title="Rule.type"></a>Rule.type</h4><p>设置匹配模块的类型，防止 <code>webpack</code> 自定义的行为发生，常用于自定义loader加载模块</p>
<h4 id="Rule-resolve"><a href="#Rule-resolve" class="headerlink" title="Rule.resolve"></a>Rule.resolve</h4><p>模块解析可以在模块层被配置, <code>Rule</code>层面的 <code>resolve</code> 会覆盖顶层的 <code>resolve配置</code></p>
<h3 id="UseEntry"><a href="#UseEntry" class="headerlink" title="UseEntry"></a>UseEntry</h3><p><code>Rule</code> 传入的形式，通常可以是对象或者一个函数, 一个必选的字符串形式的 <code>loader</code> 和一个可选的 <code>option</code></p>
<p>函数形式则会接受一个加载模块的对象参数</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// 对象形式</span><br><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  <span class="hljs-built_in">module</span>: &#123;<br>    rules: [<br>      &#123;<br>        loader: <span class="hljs-string">'css-loader'</span>,<br>        options: &#123;<br>          modules: <span class="hljs-literal">true</span><br>        &#125;<br>      &#125;<br>    ]<br>  &#125;<br>&#125;;<br><br><span class="hljs-comment">// 函数形式</span><br><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  <span class="hljs-built_in">module</span>: &#123;<br>    rules: [<br>      &#123;<br>        loader: <span class="hljs-string">'file-loader'</span>,<br>        options: &#123;<br>          outputPath: <span class="hljs-string">'svgs'</span><br>        &#125;<br>      &#125;,<br>      (info) =&gt; (&#123;<br>        loader: <span class="hljs-string">'svgo-loader'</span>,<br>        options: &#123;<br>          plugins: [&#123;<br>            cleanupIDs: &#123; <span class="hljs-attr">prefix</span>: basename(info.resource) &#125;<br>          &#125;]<br>        &#125;<br>      &#125;)<br>    ]<br>  &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<h3 id="noParse"><a href="#noParse" class="headerlink" title="noParse"></a>noParse</h3><p>不解析所传递的正则表达式匹配到的文件，这里特别注意不匹配的文件不能存在<code>require</code>和<code>import</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  <span class="hljs-built_in">module</span>: &#123;<br>    noParse: <span class="hljs-regexp">/jquery|lodash/</span>,<br>  &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<h3 id="unsafeCache"><a href="#unsafeCache" class="headerlink" title="unsafeCache"></a>unsafeCache</h3><p>缓存模块是否需要解析， 首先得保证缓存开启。缓存未开启，默认为<code>false</code>。缓存开启，默认会解析<code>node_modules</code>的模块。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  <span class="hljs-built_in">module</span>: &#123;<br>    cache: <span class="hljs-literal">true</span>,<br>    <span class="hljs-comment">// unsafeCache</span><br>  &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<h2 id="Resolve"><a href="#Resolve" class="headerlink" title="Resolve"></a>Resolve</h2><p><code>resolve</code>的作用在于如何正确的识别模块，找到需要引入<code>bundle</code>的模块代码</p>
<h3 id="alias"><a href="#alias" class="headerlink" title="alias"></a>alias</h3><p>创建别名，使得引入方式更加简单</p>
<p>也可以在给定对象的键后的末尾添加 <code>$</code>，以表示精准匹配</p>
<p>也可以使用 <code>{ &#39;路径&#39;: false }</code> 表示忽略路径的模块</p>
<blockquote>
<p>resolve.alias 优先级高于其它模块解析方式。 </p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> path = <span class="hljs-built_in">require</span>(<span class="hljs-string">'path'</span>);<br><br><span class="hljs-built_in">module</span>.exports = &#123;<br>  <span class="hljs-comment">//...</span><br>  resolve: &#123;<br>    alias: &#123;<br>      Utilities: path.resolve(__dirname, <span class="hljs-string">'src/utilities/'</span>),<br>      Templates: path.resolve(__dirname, <span class="hljs-string">'src/templates/'</span>),<br>    &#125;,<br>  &#125;,<br>&#125;;<br><br><span class="hljs-comment">// 原本方式</span><br><span class="hljs-keyword">import</span> Utility <span class="hljs-keyword">from</span> <span class="hljs-string">'../../utilities/utility'</span>;<br><br><span class="hljs-comment">// 现阶段方式</span><br><span class="hljs-keyword">import</span> Utility <span class="hljs-keyword">from</span> <span class="hljs-string">'Utilities/utility'</span>;<br></code></pre></td></tr></table></figure>

<h2 id="Optimization"><a href="#Optimization" class="headerlink" title="Optimization"></a>Optimization</h2><p>优化，会根据打包模式自动切换配置，也可以手动配置</p>
<h3 id="minimize"><a href="#minimize" class="headerlink" title="minimize"></a>minimize</h3><p>开启表示需要压缩，需要配合 <code>TerserPlugin</code> 或其它在 <code>optimization.minimizer</code> 定义的插件压缩 <code>bundle</code></p>
<h3 id="minimizer"><a href="#minimizer" class="headerlink" title="minimizer"></a>minimizer</h3><p>使用自定义压缩工具，覆盖默认压缩工具，通常使用 <code>TerserPlugin</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>    optimization: &#123;<br>    minimizer: [<br>      <span class="hljs-keyword">new</span> TerserPlugin(&#123;<br>        cache: <span class="hljs-literal">true</span>,<br>        parallel: <span class="hljs-literal">true</span>,<br>        sourceMap: <span class="hljs-literal">true</span>, <span class="hljs-comment">// 如果在生产环境中使用 source-maps，必须设置为 true</span><br>        terserOptions: &#123;<br>          <span class="hljs-comment">// https://github.com/webpack-contrib/terser-webpack-plugin#terseroptions</span><br>        &#125;<br>      &#125;),<br>    ],<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>
<p>函数形式，形参为 <code>compiler</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">module</span>.exports = &#123;<br>  optimization: &#123;<br>    minimizer: [<br>      (compiler) =&gt; &#123;<br>        <span class="hljs-keyword">const</span> TerserPlugin = <span class="hljs-built_in">require</span>(<span class="hljs-string">'terser-webpack-plugin'</span>);<br>        <span class="hljs-keyword">new</span> TerserPlugin(&#123; <span class="hljs-comment">/* 你的选项 */</span> &#125;).apply(compiler);<br>      &#125;<br>    ],<br>  &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<h3 id="splitChunks"><a href="#splitChunks" class="headerlink" title="splitChunks"></a>splitChunks</h3><p>默认使用 <code>webpack4</code>默认的分块策略，具体手动配置参考 <a href="`https://webpack.docschina.org/plugins/split-chunks-plugin/`">splitChunkPlugin</a></p>
<h3 id="runtimeChunk"><a href="#runtimeChunk" class="headerlink" title="runtimeChunk"></a>runtimeChunk</h3><p><code>runtimeChunk</code>作用在于将只含有<code>runtime</code>的入口文件单独打包一个文件， 所谓<code>runtime</code>就是指异步加载代码如<code>import(&#39;./utils&#39;) =&gt; {}</code></p>
<h3 id="emitOnErrors"><a href="#emitOnErrors" class="headerlink" title="emitOnErrors"></a>emitOnErrors</h3><p>编译时每当有错误时，会被 <code>emit</code>， 关键错误会被 <code>emit</code> 到代码中</p>
<p>todo</p>
<h2 id="Plugins"><a href="#Plugins" class="headerlink" title="Plugins"></a>Plugins</h2><h2 id="DevServer"><a href="#DevServer" class="headerlink" title="DevServer"></a>DevServer</h2><h2 id="Devtool"><a href="#Devtool" class="headerlink" title="Devtool"></a>Devtool</h2><h2 id="Targets"><a href="#Targets" class="headerlink" title="Targets"></a>Targets</h2><h2 id="watch"><a href="#watch" class="headerlink" title="watch"></a>watch</h2><h2 id="externals"><a href="#externals" class="headerlink" title="externals"></a>externals</h2><h2 id="performance"><a href="#performance" class="headerlink" title="performance"></a>performance</h2><h2 id="Node"><a href="#Node" class="headerlink" title="Node"></a>Node</h2><h2 id="stats对象"><a href="#stats对象" class="headerlink" title="stats对象"></a>stats对象</h2><h2 id="cache"><a href="#cache" class="headerlink" title="cache"></a>cache</h2>]]></content>
      <categories>
        <category>webpack 打包工具</category>
      </categories>
      <tags>
        <tag>前端</tag>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>手把手搭建一个React应用程序</title>
    <url>/2021/01/10/%E6%90%AD%E5%BB%BA%E9%A1%B9%E7%9B%AE/</url>
    <content><![CDATA[<h1 id="手把手搭建一个React应用程序"><a href="#手把手搭建一个React应用程序" class="headerlink" title="手把手搭建一个React应用程序"></a>手把手搭建一个React应用程序</h1><p>本应用程序为<code>webpack5.0</code> + <code>React17</code> + <code>antd4.x</code></p>
<h2 id="搭建环境"><a href="#搭建环境" class="headerlink" title="搭建环境"></a>搭建环境</h2><h3 id="初始化项目"><a href="#初始化项目" class="headerlink" title="初始化项目"></a>初始化项目</h3><h4 id="初始化包"><a href="#初始化包" class="headerlink" title="初始化包"></a>初始化包</h4><p>我们使用<code>npm</code>先创建一个<code>package.json</code>, 安装<code>webpack</code>&nbsp;&nbsp;&nbsp;  <code>webpack-cli</code> &nbsp;&nbsp;&nbsp;<code>react</code> &nbsp;&nbsp;&nbsp; <code>react-dom</code> &nbsp;&nbsp;&nbsp; <code>react-router-dom</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">npm init -y<br><br>yarn add webpack webpack-cli --dev<br><br>yarn add react react-dom react-router-dom<br></code></pre></td></tr></table></figure>

<p>根目录创建<code>webpack.config.js</code>(暂时只考虑<strong>开发环境</strong>)</p>
<p>根目录下创建<code>app</code>文件夹作为我们的应用程序主入口,入口文件为<code>app.js</code></p>
<p>项目结构如下</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs plain">├─ app<br>   |-app.js<br>├─ node_modules<br>    |- ......<br>├─ webpack.config.js<br>└─ package.json<br></code></pre></td></tr></table></figure>

<a id="more"></a>

<h4 id="配置基本webpack"><a href="#配置基本webpack" class="headerlink" title="配置基本webpack"></a>配置基本webpack</h4><p>这里我们配置一些基本的<code>webpack</code>打包必须的配置项</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> path = <span class="hljs-built_in">require</span>(<span class="hljs-string">"path"</span>)<br><br><span class="hljs-built_in">module</span>.exports = &#123;<br>    entry: &#123; <br>      index: &#123;<br>        <span class="hljs-keyword">import</span>: <span class="hljs-string">'/app/app.js'</span>,<br>      &#125;,<br>    &#125;,<br>    mode: <span class="hljs-string">"development"</span>,<br>    output: &#123;<br>        filename: <span class="hljs-string">'[name].[contenthash].bundle.js'</span>,<br>        chunkFilename: <span class="hljs-string">'[name].[id].js'</span>,<br>        publicPath: <span class="hljs-string">'/'</span>,<br>        path: path.resolve(__dirname, <span class="hljs-string">'dist'</span>),<br>    &#125;,<br>    devtool: <span class="hljs-string">'inline-source-map'</span>,<br>&#125;<br></code></pre></td></tr></table></figure>
<h4 id="管理资源"><a href="#管理资源" class="headerlink" title="管理资源"></a>管理资源</h4><p>我们需要解析模块, 生成<code>bundle</code>, 这里我们使用<code>babel-loader</code>来转换js, <code>less-loader</code> &nbsp;&nbsp;&nbsp; <code>css-loader</code> &nbsp;&nbsp;&nbsp; <code>style-loader</code>来加载样式</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">yarn add less less-loader --dev<br>yarn add babel-loader --dev<br>yarn add css-loader style-loader --dev<br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> path = <span class="hljs-built_in">require</span>(<span class="hljs-string">"path"</span>)<br><br><span class="hljs-built_in">module</span>.exports = &#123;<br>    entry: &#123; <br>      index: &#123;<br>        <span class="hljs-keyword">import</span>: <span class="hljs-string">'/app/app.js'</span>,<br>      &#125;,<br>    &#125;,<br>    mode: <span class="hljs-string">"development"</span>,<br>    output: &#123;<br>        filename: <span class="hljs-string">'[name].[contenthash].bundle.js'</span>,<br>        chunkFilename: <span class="hljs-string">'[name].[id].js'</span>,<br>        publicPath: <span class="hljs-string">'/'</span>,<br>        path: path.resolve(__dirname, <span class="hljs-string">'dist'</span>),<br>    &#125;,<br>    devtool: <span class="hljs-string">'inline-source-map'</span>,<br>    <span class="hljs-built_in">module</span>: &#123;<br>        rules: [<br>          &#123;<br>            test: <span class="hljs-regexp">/\.js$/</span>,<br>            exclude: <span class="hljs-regexp">/(node_modules)/</span>,<br>            include: path.resolve(__dirname, <span class="hljs-string">'app'</span>),<br>            use: &#123;<br>              loader: <span class="hljs-string">'babel-loader'</span>,<br>              options: &#123;<br>                presets: [<span class="hljs-string">'@babel/preset-env'</span>]<br>              &#125;<br>            &#125;<br>          &#125;,<br>          &#123;<br>            test: <span class="hljs-regexp">/\.less$/i</span>,<br>            use: [<span class="hljs-string">'style-loader'</span>, <span class="hljs-string">'css-loader'</span>, <span class="hljs-string">'less-loader'</span>],<br>          &#125;,<br>          &#123;<br>            test: <span class="hljs-regexp">/\.css$/i</span>,<br>            use: [<span class="hljs-string">'style-loader'</span>, <span class="hljs-string">'css-loader'</span>],<br>          &#125;,<br>        ],<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>由于用到了<code>babel</code>转换<code>js</code>,我们需要装一些<code>babel</code>的核心模块</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">yarn add @babel/core @babel/cli @babel/preset-env --dev<br></code></pre></td></tr></table></figure>

<h4 id="添加调试基础插件"><a href="#添加调试基础插件" class="headerlink" title="添加调试基础插件"></a>添加调试基础插件</h4><p>接下来装一些便于我们调试的基础插件<code>html-webpack-plugin</code> &nbsp;&nbsp;&nbsp;<code>clean-webpack-plugin</code> &nbsp;&nbsp;&nbsp;<code>webpack-manifest-plugin</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">yarn add html-webpack-plugin clean-webpack-plugin webpack-manifest-plugin --dev<br></code></pre></td></tr></table></figure>

<p>创建模板<code>index.html</code>, 并添加插件</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">html</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">head</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">meta</span> <span class="hljs-attr">charset</span>=<span class="hljs-string">"utf-8"</span> /&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">title</span>&gt;</span>起步<span class="hljs-tag">&lt;/<span class="hljs-name">title</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">head</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">body</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">id</span>=<span class="hljs-string">"app"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">body</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">html</span>&gt;</span><br></code></pre></td></tr></table></figure>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// webpack.config.js</span><br><span class="hljs-keyword">const</span> HtmlWebpackPlugin = <span class="hljs-built_in">require</span>(<span class="hljs-string">'html-webpack-plugin'</span>);<br><span class="hljs-keyword">const</span> &#123; CleanWebpackPlugin &#125; = <span class="hljs-built_in">require</span>(<span class="hljs-string">'clean-webpack-plugin'</span>);<br><span class="hljs-keyword">const</span> &#123; WebpackManifestPlugin &#125; = <span class="hljs-built_in">require</span>(<span class="hljs-string">'webpack-manifest-plugin'</span>);<br><br><span class="hljs-built_in">module</span>.exports = &#123;<br>    entry: &#123; <br>      index: &#123;<br>        <span class="hljs-keyword">import</span>: <span class="hljs-string">'/app/app.js'</span>,<br>      &#125;,<br>    &#125;,<br>    mode: <span class="hljs-string">"development"</span>,<br>    output: &#123;<br>        filename: <span class="hljs-string">'[name].[contenthash].bundle.js'</span>,<br>        chunkFilename: <span class="hljs-string">'[name].[id].js'</span>,<br>        publicPath: <span class="hljs-string">'/'</span>,<br>        path: path.resolve(__dirname, <span class="hljs-string">'dist'</span>),<br>    &#125;,<br>    devtool: <span class="hljs-string">'eval-source-map'</span>,<br>    devServer: &#123;<br>      contentBase: <span class="hljs-string">'/dist'</span>,<br>    &#125;,<br>    <span class="hljs-built_in">module</span>: &#123;<br>        rules: [<br>          &#123;<br>            test: <span class="hljs-regexp">/\.js$/</span>,<br>            exclude: <span class="hljs-regexp">/(node_modules)/</span>,<br>            include: path.resolve(__dirname, <span class="hljs-string">'app'</span>),<br>            use: &#123;<br>              loader: <span class="hljs-string">'babel-loader'</span>,<br>              options: &#123;<br>                presets: [<span class="hljs-string">'@babel/preset-env'</span>]<br>              &#125;<br>            &#125;<br>          &#125;,<br>          &#123;<br>            test: <span class="hljs-regexp">/\.less$/i</span>,<br>            use: [<span class="hljs-string">'style-loader'</span>, <span class="hljs-string">'css-loader'</span>, <span class="hljs-string">'less-loader'</span>],<br>          &#125;,<br>          &#123;<br>            test: <span class="hljs-regexp">/\.css$/i</span>,<br>            use: [<span class="hljs-string">'style-loader'</span>, <span class="hljs-string">'css-loader'</span>],<br>          &#125;,<br>        ],<br>    &#125;,<br>    plugins: [<br>      <span class="hljs-keyword">new</span> CleanWebpackPlugin(),<br>      <span class="hljs-keyword">new</span> HtmlWebpackPlugin(&#123;<br>        inject: <span class="hljs-literal">true</span>,<br>        template: <span class="hljs-string">'/app/index.html'</span>,<br>      &#125;),<br>      <span class="hljs-keyword">new</span> WebpackManifestPlugin()<br>    ],<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="创建服务"><a href="#创建服务" class="headerlink" title="创建服务"></a>创建服务</h4><p>新建本地服务, 添加模块热更新, 这里我们使用<code>Express</code>启动服务</p>
<p>我们在根目录创建一个<code>server</code>的文件夹, 创建一个<code>index.js</code>文件, 编写我们的启动脚本</p>
<p>这里我们需要安装<code>webpack-dev-server</code> &nbsp;&nbsp;&nbsp; <code>webpack-dev-middleware</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">yarn add express<br>yarn add webpack-dev-server --dev<br>yarn add webpack-dev-middleware --dev<br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// /server/index.js</span><br><span class="hljs-keyword">const</span> express = <span class="hljs-built_in">require</span>(<span class="hljs-string">'express'</span>);<br><span class="hljs-keyword">const</span> webpack = <span class="hljs-built_in">require</span>(<span class="hljs-string">'webpack'</span>);<br><span class="hljs-keyword">const</span> webpackDevMiddleware = <span class="hljs-built_in">require</span>(<span class="hljs-string">'webpack-dev-middleware'</span>);<br><br><span class="hljs-keyword">const</span> app = express();<br><span class="hljs-keyword">const</span> config = <span class="hljs-built_in">require</span>(<span class="hljs-string">'./webpack.config.js'</span>);<br><span class="hljs-keyword">const</span> compiler = webpack(config);<br><br><span class="hljs-comment">// 告知 express 使用 webpack-dev-middleware，</span><br><span class="hljs-comment">// 以及将 webpack.config.js 配置文件作为基础配置。</span><br>app.use(<br>  webpackDevMiddleware(compiler, &#123;<br>    publicPath: config.output.publicPath,<br>  &#125;)<br>);<br><br><span class="hljs-comment">// 将文件 serve 到 port 3000。</span><br>app.listen(<span class="hljs-number">3000</span>, <span class="hljs-function"><span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) </span>&#123;<br>  <span class="hljs-built_in">console</span>.log(<span class="hljs-string">'Example app listening on port 3000!\n'</span>);<br>&#125;);<br></code></pre></td></tr></table></figure>

<p><code>webpack.config.js</code>文件添加<code>contentBase</code>,表明从哪里去寻找文件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">devServer: &#123;<br>    contentBase: <span class="hljs-string">'./dist'</span>, <span class="hljs-comment">// dist为我们的输出目录</span><br>&#125;,<br></code></pre></td></tr></table></figure>

<h4 id="编写启动脚本"><a href="#编写启动脚本" class="headerlink" title="编写启动脚本"></a>编写启动脚本</h4><p>我们在<code>package.json</code>中编写一些脚本,便于我们启动服务和打包</p>
<figure class="highlight"><table><tr><td class="code"><pre><code class="hljs json">"scripts": &#123;<br>    "test": "echo \"Error: no test specified\" &amp;&amp; exit 1",<br>    "build": "webpack --config webpack.config.js",<br>    "start": "node server/index.js"<br>&#125;,<br></code></pre></td></tr></table></figure>

<p>这时候基本的需要已经搭建完成, 我们在<code>app.js</code>中随便写点东西</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">console</span>.log(<span class="hljs-number">123</span>)<br></code></pre></td></tr></table></figure>
<p><img src="webpack%E5%90%AF%E5%8A%A8.png" alt=""><br><img src="%E5%90%AF%E5%8A%A8%E9%A1%B5%E9%9D%A2.png" alt=""></p>
<h3 id="解析jsx"><a href="#解析jsx" class="headerlink" title="解析jsx"></a>解析jsx</h3><p>到此为止我们已经可以正确打包js文件, 正确启动脚本, 安装了应用程序一些必要的依赖, 但是我们还无法解析<code>jsx</code>语法</p>
<p>如果我们在app.js文件中写如下代码, 启动或编译</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// app.js</span><br><br><span class="hljs-keyword">import</span> React <span class="hljs-keyword">from</span> <span class="hljs-string">'react'</span><br><span class="hljs-keyword">import</span> ReactDOM <span class="hljs-keyword">from</span> <span class="hljs-string">'react-dom'</span><br><br>ReactDOM.render(<span class="xml"><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>111<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span></span>, <span class="hljs-built_in">document</span>.getElementById(<span class="hljs-string">"app"</span>))<br></code></pre></td></tr></table></figure>
<p>我们会看到如下报错<br><img src="jsx%E6%8A%A5%E9%94%99.png" alt=""></p>
<p>因此我们需要安装<code>babel</code>转换jsx相关的插件</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">yarn add @babel/preset-react --dev<br></code></pre></td></tr></table></figure>

<p>按照<code>babel</code>官网的教程,在根目录下创建<code>babel.config.json</code></p>
<p>此时项目结构如下</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><code class="hljs plain">├─ app<br>   |-app.js<br>├─ node_modules<br>    |- ......<br>├─ server<br>    ├─ index.js<br>├─ webpack.config.js<br>├─ babel.config.json<br>└─ package.json<br>└─ yarn.lock<br></code></pre></td></tr></table></figure>

<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-comment">// babel.config.json</span><br>&#123;<br>    <span class="hljs-attr">"presets"</span>: [<br>      [<br>        <span class="hljs-string">"@babel/env"</span>,<br>        &#123;<br>          <span class="hljs-attr">"useBuiltIns"</span>: <span class="hljs-string">"usage"</span><br>        &#125;<br>      ],<br>      [<span class="hljs-string">"@babel/preset-react"</span>, <br>        &#123;<br>          <span class="hljs-attr">"development"</span>: <span class="hljs-literal">true</span><br>        &#125;<br>      ]<br>    ],<br>&#125;<br></code></pre></td></tr></table></figure>
<p>这里注意,<code>babel</code> 中<code>preset</code>顺序是从后往前执行,<code>plugin</code>的顺序是从后往前执行</p>
<p>再次打包后成功<br><img src="babel%E8%A7%A3%E6%9E%90jsx.png" alt=""></p>
<p>我们启动下试试看<br><img src="babel%E8%BD%AC%E6%8D%A2jsx%E6%88%90%E5%8A%9F" alt=""></p>
<p>同样, 使用<code>class</code>的写法也能正确解析, 这里有个注意点, 针对类, 我们需要使用<code>@babel/plugin-proposal-class-properties</code>,否则如下代码无法编译</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">A</span> </span>&#123;<br>    say = <span class="hljs-function"><span class="hljs-params">()</span> =&gt;</span> &#123;<br>        <span class="hljs-built_in">console</span>.log(<span class="hljs-string">'我说话了'</span>)<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">let</span> a = <span class="hljs-keyword">new</span> A()<br>a.say()<br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">yarn add @babel/plugin-proposal-<span class="hljs-class"><span class="hljs-keyword">class</span>-<span class="hljs-title">properties</span> --<span class="hljs-title">dev</span></span><br></code></pre></td></tr></table></figure>

<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-comment">// babel.config.json</span><br>&#123;<br>    <span class="hljs-attr">"presets"</span>: [<br>      [<br>        <span class="hljs-string">"@babel/env"</span>,<br>        &#123;<br>          <span class="hljs-attr">"useBuiltIns"</span>: <span class="hljs-string">"usage"</span><br>        &#125;<br>      ],<br>      [<span class="hljs-string">"@babel/preset-react"</span>, <br>        &#123;<br>          <span class="hljs-attr">"development"</span>: <span class="hljs-literal">true</span><br>        &#125;<br>      ]<br>    ],<br>    <span class="hljs-attr">"plugins"</span>: [<br>        [<span class="hljs-string">"@babel/plugin-proposal-class-properties"</span>, &#123; <span class="hljs-attr">"loose"</span>: <span class="hljs-literal">true</span> &#125;]<br>    ]<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="安装antd"><a href="#安装antd" class="headerlink" title="安装antd"></a>安装antd</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">yarn add antd<br></code></pre></td></tr></table></figure>
<p>我们修改<code>app.js</code>,导入<code>antd</code>的<code>Button</code>看看效果如何</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// app.js</span><br><span class="hljs-keyword">import</span> React <span class="hljs-keyword">from</span> <span class="hljs-string">'react'</span><br><span class="hljs-keyword">import</span> ReactDOM <span class="hljs-keyword">from</span> <span class="hljs-string">'react-dom'</span><br><span class="hljs-keyword">import</span> &#123; Button &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">'antd'</span><br><br><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MyDemo</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">React</span>.<span class="hljs-title">Component</span></span>&#123;<br>    <span class="hljs-keyword">constructor</span>(props)&#123;<br>        <span class="hljs-keyword">super</span>(props)<br>        <span class="hljs-keyword">this</span>.state = &#123;<br>            buttonText: <span class="hljs-string">"我是按钮"</span><br>        &#125;<br>    &#125;<br><br>    render()&#123;<br>        <span class="hljs-keyword">return</span> <span class="xml"><span class="hljs-tag">&lt;<span class="hljs-name">Button</span>&gt;</span>&#123;this.state.buttonText&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">Button</span>&gt;</span></span><br>    &#125;<br>&#125;<br><br>ReactDOM.render(<span class="xml"><span class="hljs-tag">&lt;<span class="hljs-name">MyDemo</span> /&gt;</span></span>, <span class="hljs-built_in">document</span>.getElementById(<span class="hljs-string">"app"</span>))<br></code></pre></td></tr></table></figure>
<p>结果是确实引入了<code>antd</code>的组件,但是并未引入<code>antd</code>的样式</p>
<p>我们选择使用<code>babel</code>动态导入的方法引入样式</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><code class="hljs shell">yarn add babel-plugin-import --dev<br></code></pre></td></tr></table></figure>

<p>然后修改<code>babel.config.json</code>配置</p>
<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-comment">// babel.config.json</span><br>&#123;<br>    <span class="hljs-attr">"presets"</span>: [<br>      [<br>        <span class="hljs-string">"@babel/env"</span>,<br>        &#123;<br>          <span class="hljs-attr">"useBuiltIns"</span>: <span class="hljs-string">"usage"</span><br>        &#125;<br>      ],<br>      [<span class="hljs-string">"@babel/preset-react"</span>, <br>        &#123;<br>          <span class="hljs-attr">"development"</span>: <span class="hljs-literal">true</span><br>        &#125;<br>      ]<br>    ],<br>    <span class="hljs-attr">"plugins"</span>: [<br>        [<span class="hljs-string">"@babel/plugin-proposal-class-properties"</span>, &#123; <span class="hljs-attr">"loose"</span>: <span class="hljs-literal">true</span> &#125;],<br>        [<span class="hljs-string">"import"</span>, &#123; <span class="hljs-attr">"libraryName"</span>: <span class="hljs-string">"antd"</span>, <span class="hljs-attr">"libraryDirectory"</span>: <span class="hljs-string">"es"</span>, <span class="hljs-attr">"style"</span>: <span class="hljs-string">"css"</span> &#125;]<br>    ]<br>&#125;<br></code></pre></td></tr></table></figure>
<p>结果如下</p>
<p>!()[antd样式注入成功.png]</p>
]]></content>
      <categories>
        <category>React</category>
      </categories>
      <tags>
        <tag>React</tag>
        <tag>前端</tag>
        <tag>webpack</tag>
      </tags>
  </entry>
</search>
